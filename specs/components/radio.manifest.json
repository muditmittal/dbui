{
  "$component": "Radio",
  "$figmaId": "715:650",
  "$figmaUrl": "https://www.figma.com/design/OftbSQf85jOPln9RhSEhVv?node-id=715-650",
  "$codePath": "packages/dbui/src/components/ui/radio-group.tsx",
  "$storyPath": "apps/portal/src/stories/RadioGroup.stories.tsx",
  "variants": {
    "Selected": {
      "figma": ["False", "True"],
      "code": "data-checked attribute (Base UI)"
    },
    "State": {
      "figma": ["Default", "Hover", "Press", "Focus", "Disabled"],
      "code": ["CSS hover", "CSS active", "focus-visible", "disabled prop"]
    }
  },
  "guidelines": [
    "Use when only one option can be selected from a group",
    "Default to the most common or recommended option",
    "List options in logical order (alphabetical, frequency, or severity)"
  ],
  "constraints": [
    "Minimum 2 options — for on/off use Switch instead",
    "Label text must use Paragraph style (13px Regular)",
    "Never use radio buttons for actions — they're for selection only"
  ]
}
