import { Meta } from "@storybook/blocks"
import { HomePage } from "./components/HomePage"

{/*
  The nav's grounding point. A docs entry, like Components/Overview, so the
  controls panel stays out of the way; `sb-unstyled` keeps Storybook's docs
  stylesheet from restyling DBUI's own components.
*/}

<Meta title="Home" />

<div className="sb-unstyled">
  <HomePage />
</div>
