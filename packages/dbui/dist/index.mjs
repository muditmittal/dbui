export { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText, InputGroupTextarea } from './chunk-6L6ZLWJL.mjs';
export { KeyValueGrid, KeyValueItem, KeyValueKey, KeyValuePair, KeyValueTitle, KeyValueValue, KeyValueValueEnd } from './chunk-MJUOE3IO.mjs';
export { EditorTab, EditorTabAddButton, EditorTabIcon, EditorTabs } from './chunk-B7FZAVRG.mjs';
export { Spinner } from './chunk-OI5GGYKE.mjs';
export { Progress, ProgressIndicator, ProgressLabel, ProgressTrack, ProgressValue } from './chunk-V6WKMRYE.mjs';
export { Pagination, PaginationContent, PaginationEllipsis, PaginationItem, PaginationLink, PaginationNext, PaginationPrevious } from './chunk-BEOEKMG2.mjs';
export { Tag, TagIcon, TagLabel, TagRemove, TagValue } from './chunk-4GTRFJCG.mjs';
export { Status } from './chunk-SHAKWHAZ.mjs';
export { Empty, EmptyContent, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from './chunk-KDIGCFSO.mjs';
export { HoverCard, HoverCardContent, HoverCardTrigger } from './chunk-YH7A3AKI.mjs';
export { RadioTile, RadioTileDescription, RadioTileGroup, RadioTileHeader, RadioTileIcon, RadioTileTitle } from './chunk-3GXKQPOC.mjs';
import { Label } from './chunk-NEJMPCKW.mjs';
export { Label } from './chunk-NEJMPCKW.mjs';
export { Textarea } from './chunk-TPBUBAPY.mjs';
export { Slider } from './chunk-INISHWOT.mjs';
import './chunk-FQY64KH3.mjs';
export { FilterToggle, Toggle, toggleVariants } from './chunk-AHNDU2UE.mjs';
import { ToggleGroup } from './chunk-HJQ77CEE.mjs';
export { SegmentControl, SegmentControlItem } from './chunk-HJQ77CEE.mjs';
import { Toggle } from './chunk-XSEYRSXR.mjs';
export { SplitButton, SplitButtonSeparator, SplitButtonText, splitButtonVariants } from './chunk-2VNOCX4M.mjs';
import { Separator as Separator$1 } from './chunk-6GJBCEWE.mjs';
export { Separator } from './chunk-6GJBCEWE.mjs';
export { Skeleton } from './chunk-GUX4HNE5.mjs';
import { index_parts_exports as index_parts_exports$1 } from './chunk-CPCPI5H4.mjs';
export { Popover, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger } from './chunk-CPCPI5H4.mjs';
export { Tooltip, TooltipArrow, TooltipContent, TooltipProvider, TooltipTrigger } from './chunk-NDRZRKSZ.mjs';
import './chunk-DNSQT2H3.mjs';
export { Drawer, DrawerClose, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerOverlay, DrawerPortal, DrawerTitle, DrawerTrigger } from './chunk-X24MV7J3.mjs';
export { Navbar, NavbarItem, NavbarItemIcon, NavbarNewButton, NavbarSection, NavbarSectionHeader } from './chunk-AO7BCWJC.mjs';
import { collapsibleOpenStateMapping, useCollapsibleRoot, CollapsibleRootContext, triggerOpenStateMapping, useCollapsibleRootContext, useCollapsiblePanel } from './chunk-XKWIIYRX.mjs';
export { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from './chunk-XKWIIYRX.mjs';
export { Avatar, AvatarBadge, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarImage } from './chunk-TD374PSM.mjs';
export { Badge, badgeVariants } from './chunk-CLHOC6TY.mjs';
export { Breadcrumb, BreadcrumbEllipsis, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from './chunk-K4IF3ZCU.mjs';
import { Overflow } from './chunk-4XDLRFJP.mjs';
import { useRender } from './chunk-TVZRNYA7.mjs';
export { Dialog, DialogClose, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogHeaderIcon, DialogOverlay, DialogPortal, DialogTitle, DialogTrigger } from './chunk-YPVB3F3A.mjs';
export { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogIcon, AlertDialogOverlay, AlertDialogPortal, AlertDialogTitle, AlertDialogTrigger } from './chunk-6K33DUY4.mjs';
import './chunk-4FUS5W2N.mjs';
import { MenuSubmenuTrigger, MenuSubmenuRoot, MenuRadioItemIndicator, MenuRadioItem, MenuRadioGroup, MenuPositioner, MenuPortal, MenuPopup, MenuLinkItem, MenuItem, MenuGroupLabel, MenuGroup, MenuCheckboxItemIndicator, MenuCheckboxItem, MenuBackdrop, MenuArrow, ContextMenuRootContext, MenuRootContext, index_parts_exports, useContextMenuRootContext, useMenuRootContext, findRootOwnerId, MenubarContext, useMenubarContext, DropdownMenu, DropdownMenuGroup, DropdownMenuPortal, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuRadioGroup, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuShortcut, DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent } from './chunk-S4KMIGXU.mjs';
export { DropdownMenu, DropdownMenuCheckboxItem, DropdownMenuContent, DropdownMenuEmpty, DropdownMenuFooter, DropdownMenuGroup, DropdownMenuItem, DropdownMenuItemBadge, DropdownMenuItemDescription, DropdownMenuItemIcon, DropdownMenuLabel, DropdownMenuLoading, DropdownMenuPortal, DropdownMenuRadioGroup, DropdownMenuRadioItem, DropdownMenuSearch, DropdownMenuSeparator, DropdownMenuShortcut, DropdownMenuSub, DropdownMenuSubContent, DropdownMenuSubTrigger, DropdownMenuTrigger } from './chunk-S4KMIGXU.mjs';
export { DirectionProvider } from './chunk-F6U7T6AJ.mjs';
import { ChevronRight } from './chunk-SKOBVIBK.mjs';
import './chunk-2ZE5P47Q.mjs';
export { Tabs, TabsContent, TabsIcon, TabsList, TabsTrigger, tabsListVariants } from './chunk-P7OGOIBG.mjs';
import './chunk-R5XIMWTD.mjs';
export { Table, TableBody, TableCaption, TableCell, TableCellExpandable, TableCellIcon, TableCellMeta, TableCellStatus, TableCellTime, TableCellTitle, TableCellTitleContent, TableCellUser, TableFooter, TableHead, TableHeader, TableRow, TableSortButton } from './chunk-KTDI5YUZ.mjs';
export { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from './chunk-Q6A3SSX3.mjs';
export { Alert, AlertAction, AlertClose, AlertContent, AlertDescription, AlertIcon, AlertTitle } from './chunk-X6AYERF7.mjs';
export { Toaster } from './chunk-KSIAJ4B4.mjs';
export { Button, ButtonChevron, ButtonIcon } from './chunk-RQBQDFLU.mjs';
export { buttonVariants } from './chunk-DIGGK24S.mjs';
import { cva } from './chunk-7TQTDX5Q.mjs';
import './chunk-6R4B2ATK.mjs';
import { Input } from './chunk-RALFBPK4.mjs';
export { Input } from './chunk-RALFBPK4.mjs';
import { styleDisableScrollbar } from './chunk-IF3VZH4N.mjs';
export { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue } from './chunk-IF3VZH4N.mjs';
import { clamp } from './chunk-NNFC4ROP.mjs';
import './chunk-Z4QJVOMC.mjs';
import './chunk-T7CEVU6N.mjs';
import { useCSPContext } from './chunk-WUPFH4N7.mjs';
export { Combobox, ComboboxChip, ComboboxChips, ComboboxChipsInput, ComboboxCollection, ComboboxContent, ComboboxEmpty, ComboboxGroup, ComboboxInput, ComboboxItem, ComboboxLabel, ComboboxList, ComboboxSeparator, ComboboxTrigger, ComboboxValue, useComboboxAnchor } from './chunk-TM7HHQHU.mjs';
import './chunk-IL2HV3NS.mjs';
import './chunk-45GJTJ2K.mjs';
import './chunk-B4K6AF4V.mjs';
import './chunk-OOCSARU7.mjs';
import { ChevronDown } from './chunk-3L5IV4F2.mjs';
import { Check } from './chunk-JN7JP22S.mjs';
import { Separator } from './chunk-VX2FCPLK.mjs';
import './chunk-75IAMYM2.mjs';
import './chunk-SVF7A3EA.mjs';
import { useOpenInteractionType, useScrollLock } from './chunk-4AAVJQFL.mjs';
import { pressableTriggerOpenStateMapping, FloatingTree, useFloatingNodeId, useFloatingTree, FloatingNode } from './chunk-PSMHWWS3.mjs';
import './chunk-RAYLQUWY.mjs';
import './chunk-POBMUUJY.mjs';
import './chunk-KY5XN6YG.mjs';
import './chunk-PL3VOM4K.mjs';
import { useTimeout } from './chunk-NJQVCWLB.mjs';
import { ownerDocument } from './chunk-SOLCGBP2.mjs';
import { Checkbox } from './chunk-RJZRDRNU.mjs';
export { Checkbox } from './chunk-RJZRDRNU.mjs';
import './chunk-4ZECPVCA.mjs';
export { RadioGroup, RadioGroupItem } from './chunk-AYMUJWFQ.mjs';
import './chunk-PTHMG4NM.mjs';
import './chunk-TM5TEJGH.mjs';
import { CompositeRoot } from './chunk-6FR5MJRV.mjs';
import './chunk-YUC6B4OH.mjs';
import './chunk-HE7MSZBA.mjs';
import './chunk-UZLC4QBY.mjs';
import './chunk-QGKCYW24.mjs';
import './chunk-PTVDKL3E.mjs';
import './chunk-X4LRWBNJ.mjs';
import './chunk-SDWHTCRY.mjs';
import { useDirection } from './chunk-NNBMTHBT.mjs';
export { useDirection } from './chunk-NNBMTHBT.mjs';
import { stopEvent } from './chunk-XK53VWW6.mjs';
import { getTarget, contains } from './chunk-FQ4RTFU7.mjs';
import { isWebKit } from './chunk-BO7ZMLYZ.mjs';
import './chunk-P2KQEZ47.mjs';
import { transitionStatusMapping, useOpenChangeComplete } from './chunk-INMEVUNJ.mjs';
import './chunk-KJU32T43.mjs';
export { Switch } from './chunk-I2OELYCB.mjs';
import './chunk-CMOB2AMB.mjs';
import './chunk-2E5W2VBA.mjs';
import './chunk-NH7KKUQN.mjs';
import './chunk-N4ATC6XY.mjs';
import './chunk-BVGGELUI.mjs';
import { useButton } from './chunk-ETMT7VCK.mjs';
import './chunk-HRT42H6K.mjs';
import './chunk-SCSP3VWU.mjs';
import './chunk-DEE5H5PA.mjs';
import { createChangeEventDetails, reason_parts_exports } from './chunk-FVYBAJVM.mjs';
import { useId, useBaseUiId } from './chunk-WIUX54UE.mjs';
import './chunk-T3HTT7SQ.mjs';
import './chunk-CL6E6FD3.mjs';
import './chunk-6PGNYKRT.mjs';
import { useStableCallback } from './chunk-3XULTTOV.mjs';
import { useIsoLayoutEffect } from './chunk-C575TH42.mjs';
import { useRenderElement, warn, formatErrorMessage_default, mergeProps } from './chunk-I44XWQG6.mjs';
import { cn } from './chunk-5ZU3S2VQ.mjs';
export { cn } from './chunk-5ZU3S2VQ.mjs';
import { __export, __spreadValues, __objRest, __spreadProps } from './chunk-LQPATFHW.mjs';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import * as React9 from 'react';
import { forwardRef, useMemo } from 'react';
import * as RechartsPrimitive from 'recharts';
import * as ResizablePrimitive from 'react-resizable-panels';

function AspectRatio(_a) {
  var _b = _a, {
    ratio,
    className
  } = _b, props = __objRest(_b, [
    "ratio",
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "aspect-ratio",
      style: {
        "--ratio": ratio
      },
      className: cn("relative aspect-(--ratio)", className)
    }, props)
  );
}
var buttonGroupVariants = cva(
  "flex w-fit items-stretch *:focus-visible:relative *:focus-visible:z-10 has-[>[data-slot=button-group]]:gap-2 has-[select[aria-hidden=true]:last-child]:[&>[data-slot=select-trigger]:last-of-type]:rounded-r-lg [&>[data-slot=select-trigger]:not([class*='w-'])]:w-fit [&>input]:flex-1",
  {
    variants: {
      orientation: {
        horizontal: "*:data-slot:rounded-r-none [&>[data-slot]:not(:has(~[data-slot]))]:rounded-r-lg! [&>[data-slot]~[data-slot]]:rounded-l-none [&>[data-slot]~[data-slot]]:border-l-0",
        vertical: "flex-col *:data-slot:rounded-b-none [&>[data-slot]:not(:has(~[data-slot]))]:rounded-b-lg! [&>[data-slot]~[data-slot]]:rounded-t-none [&>[data-slot]~[data-slot]]:border-t-0"
      }
    },
    defaultVariants: {
      orientation: "horizontal"
    }
  }
);
function ButtonGroup(_a) {
  var _b = _a, {
    className,
    orientation
  } = _b, props = __objRest(_b, [
    "className",
    "orientation"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      role: "group",
      "data-slot": "button-group",
      "data-orientation": orientation,
      className: cn(buttonGroupVariants({ orientation }), className)
    }, props)
  );
}
function ButtonGroupText(_a) {
  var _b = _a, {
    className,
    render
  } = _b, props = __objRest(_b, [
    "className",
    "render"
  ]);
  return useRender({
    defaultTagName: "div",
    props: mergeProps(
      {
        className: cn(
          "flex items-center gap-2 rounded-lg border bg-muted px-2.5 text-[13px] font-semibold [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4",
          className
        )
      },
      props
    ),
    render,
    state: {
      slot: "button-group-text"
    }
  });
}
function ButtonGroupSeparator(_a) {
  var _b = _a, {
    className,
    orientation = "vertical"
  } = _b, props = __objRest(_b, [
    "className",
    "orientation"
  ]);
  return /* @__PURE__ */ jsx(
    Separator$1,
    __spreadValues({
      "data-slot": "button-group-separator",
      orientation,
      className: cn(
        "relative self-stretch bg-input data-horizontal:mx-px data-horizontal:w-auto data-vertical:my-px data-vertical:h-auto",
        className
      )
    }, props)
  );
}
var THEMES = { light: "", dark: ".dark" };
var ChartContext = React9.createContext(null);
function useChart() {
  const context = React9.useContext(ChartContext);
  if (!context) {
    throw new Error("useChart must be used within a <ChartContainer />");
  }
  return context;
}
function ChartContainer(_a) {
  var _b = _a, {
    id,
    className,
    children,
    config
  } = _b, props = __objRest(_b, [
    "id",
    "className",
    "children",
    "config"
  ]);
  const uniqueId = React9.useId();
  const chartId = `chart-${id || uniqueId.replace(/:/g, "")}`;
  return /* @__PURE__ */ jsx(ChartContext.Provider, { value: { config }, children: /* @__PURE__ */ jsxs(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "chart",
      "data-chart": chartId,
      className: cn(
        "flex aspect-video justify-center text-[12px] [&_.recharts-cartesian-axis-tick_text]:fill-muted-foreground [&_.recharts-cartesian-grid_line[stroke='#ccc']]:stroke-border/50 [&_.recharts-curve.recharts-tooltip-cursor]:stroke-border [&_.recharts-dot[stroke='#fff']]:stroke-transparent [&_.recharts-layer]:outline-hidden [&_.recharts-polar-grid_[stroke='#ccc']]:stroke-border [&_.recharts-radial-bar-background-sector]:fill-muted [&_.recharts-rectangle.recharts-tooltip-cursor]:fill-muted [&_.recharts-reference-line_[stroke='#ccc']]:stroke-border [&_.recharts-sector]:outline-hidden [&_.recharts-sector[stroke='#fff']]:stroke-transparent [&_.recharts-surface]:outline-hidden",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(ChartStyle, { id: chartId, config }),
        /* @__PURE__ */ jsx(RechartsPrimitive.ResponsiveContainer, { children })
      ]
    })
  ) });
}
var ChartStyle = ({ id, config }) => {
  const colorConfig = Object.entries(config).filter(
    ([, config2]) => config2.theme || config2.color
  );
  if (!colorConfig.length) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "style",
    {
      dangerouslySetInnerHTML: {
        __html: Object.entries(THEMES).map(
          ([theme, prefix]) => `
${prefix} [data-chart=${id}] {
${colorConfig.map(([key, itemConfig]) => {
            var _a;
            const color = ((_a = itemConfig.theme) == null ? void 0 : _a[theme]) || itemConfig.color;
            return color ? `  --color-${key}: ${color};` : null;
          }).join("\n")}
}
`
        ).join("\n")
      }
    }
  );
};
var ChartTooltip = RechartsPrimitive.Tooltip;
function ChartTooltipContent({
  active,
  payload,
  className,
  indicator = "dot",
  hideLabel = false,
  hideIndicator = false,
  label,
  labelFormatter,
  labelClassName,
  formatter,
  color,
  nameKey,
  labelKey
}) {
  const { config } = useChart();
  const tooltipLabel = React9.useMemo(() => {
    var _a;
    if (hideLabel || !(payload == null ? void 0 : payload.length)) {
      return null;
    }
    const [item] = payload;
    const key = `${labelKey || (item == null ? void 0 : item.dataKey) || (item == null ? void 0 : item.name) || "value"}`;
    const itemConfig = getPayloadConfigFromPayload(config, item, key);
    const value = !labelKey && typeof label === "string" ? ((_a = config[label]) == null ? void 0 : _a.label) || label : itemConfig == null ? void 0 : itemConfig.label;
    if (labelFormatter) {
      return /* @__PURE__ */ jsx("div", { className: cn("font-semibold", labelClassName), children: labelFormatter(value, payload) });
    }
    if (!value) {
      return null;
    }
    return /* @__PURE__ */ jsx("div", { className: cn("font-semibold", labelClassName), children: value });
  }, [
    label,
    labelFormatter,
    payload,
    hideLabel,
    labelClassName,
    config,
    labelKey
  ]);
  if (!active || !(payload == null ? void 0 : payload.length)) {
    return null;
  }
  const nestLabel = payload.length === 1 && indicator !== "dot";
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "grid min-w-32 items-start gap-1.5 rounded-lg border border-border/50 bg-background px-2.5 py-1.5 text-[12px] shadow-xl",
        className
      ),
      children: [
        !nestLabel ? tooltipLabel : null,
        /* @__PURE__ */ jsx("div", { className: "grid gap-1.5", children: payload.filter((item) => item.type !== "none").map((item, index) => {
          const key = `${nameKey || item.name || item.dataKey || "value"}`;
          const itemConfig = getPayloadConfigFromPayload(config, item, key);
          const indicatorColor = color || item.payload.fill || item.color;
          return /* @__PURE__ */ jsx(
            "div",
            {
              className: cn(
                "flex w-full flex-wrap items-stretch gap-2 [&>svg]:h-2.5 [&>svg]:w-2.5 [&>svg]:text-muted-foreground",
                indicator === "dot" && "items-center"
              ),
              children: formatter && (item == null ? void 0 : item.value) !== void 0 && item.name ? formatter(item.value, item.name, item, index, item.payload) : /* @__PURE__ */ jsxs(Fragment, { children: [
                (itemConfig == null ? void 0 : itemConfig.icon) ? /* @__PURE__ */ jsx(itemConfig.icon, {}) : !hideIndicator && /* @__PURE__ */ jsx(
                  "div",
                  {
                    className: cn(
                      "shrink-0 rounded-[2px] border-(--color-border) bg-(--color-bg)",
                      {
                        "h-2.5 w-2.5": indicator === "dot",
                        "w-1": indicator === "line",
                        "w-0 border-[1.5px] border-dashed bg-transparent": indicator === "dashed",
                        "my-0.5": nestLabel && indicator === "dashed"
                      }
                    ),
                    style: {
                      "--color-bg": indicatorColor,
                      "--color-border": indicatorColor
                    }
                  }
                ),
                /* @__PURE__ */ jsxs(
                  "div",
                  {
                    className: cn(
                      "flex flex-1 justify-between leading-none",
                      nestLabel ? "items-end" : "items-center"
                    ),
                    children: [
                      /* @__PURE__ */ jsxs("div", { className: "grid gap-1.5", children: [
                        nestLabel ? tooltipLabel : null,
                        /* @__PURE__ */ jsx("span", { className: "text-muted-foreground", children: (itemConfig == null ? void 0 : itemConfig.label) || item.name })
                      ] }),
                      item.value && /* @__PURE__ */ jsx("span", { className: "font-mono font-semibold text-foreground tabular-nums", children: item.value.toLocaleString() })
                    ]
                  }
                )
              ] })
            },
            item.dataKey
          );
        }) })
      ]
    }
  );
}
var ChartLegend = RechartsPrimitive.Legend;
function ChartLegendContent({
  className,
  hideIcon = false,
  payload,
  verticalAlign = "bottom",
  nameKey
}) {
  const { config } = useChart();
  if (!(payload == null ? void 0 : payload.length)) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    {
      className: cn(
        "flex items-center justify-center gap-4",
        verticalAlign === "top" ? "pb-3" : "pt-3",
        className
      ),
      children: payload.filter((item) => item.type !== "none").map((item) => {
        const key = `${nameKey || item.dataKey || "value"}`;
        const itemConfig = getPayloadConfigFromPayload(config, item, key);
        return /* @__PURE__ */ jsxs(
          "div",
          {
            className: cn(
              "flex items-center gap-1.5 [&>svg]:h-3 [&>svg]:w-3 [&>svg]:text-muted-foreground"
            ),
            children: [
              (itemConfig == null ? void 0 : itemConfig.icon) && !hideIcon ? /* @__PURE__ */ jsx(itemConfig.icon, {}) : /* @__PURE__ */ jsx(
                "div",
                {
                  className: "h-2 w-2 shrink-0 rounded-[2px]",
                  style: {
                    backgroundColor: item.color
                  }
                }
              ),
              itemConfig == null ? void 0 : itemConfig.label
            ]
          },
          item.value
        );
      })
    }
  );
}
function getPayloadConfigFromPayload(config, payload, key) {
  if (typeof payload !== "object" || payload === null) {
    return void 0;
  }
  const payloadPayload = "payload" in payload && typeof payload.payload === "object" && payload.payload !== null ? payload.payload : void 0;
  let configLabelKey = key;
  if (key in payload && typeof payload[key] === "string") {
    configLabelKey = payload[key];
  } else if (payloadPayload && key in payloadPayload && typeof payloadPayload[key] === "string") {
    configLabelKey = payloadPayload[key];
  }
  return configLabelKey in config ? config[configLabelKey] : config[key];
}

// ../../node_modules/@base-ui/react/esm/collapsible/index.parts.js
var index_parts_exports3 = {};
__export(index_parts_exports3, {
  Panel: () => CollapsiblePanel,
  Root: () => CollapsibleRoot,
  Trigger: () => CollapsibleTrigger
});

// ../../node_modules/@base-ui/react/esm/collapsible/root/stateAttributesMapping.js
var collapsibleStateAttributesMapping = __spreadValues(__spreadValues({}, collapsibleOpenStateMapping), transitionStatusMapping);
var CollapsibleRoot = /* @__PURE__ */ React9.forwardRef(function CollapsibleRoot2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className,
    defaultOpen = false,
    disabled = false,
    onOpenChange: onOpenChangeProp,
    open
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className",
    "defaultOpen",
    "disabled",
    "onOpenChange",
    "open"
  ]);
  const onOpenChange = useStableCallback(onOpenChangeProp);
  const collapsible = useCollapsibleRoot({
    open,
    defaultOpen,
    onOpenChange,
    disabled
  });
  const state = React9.useMemo(() => ({
    open: collapsible.open,
    disabled: collapsible.disabled,
    transitionStatus: collapsible.transitionStatus
  }), [collapsible.open, collapsible.disabled, collapsible.transitionStatus]);
  const contextValue = React9.useMemo(() => __spreadProps(__spreadValues({}, collapsible), {
    onOpenChange,
    state
  }), [collapsible, onOpenChange, state]);
  const element = useRenderElement("div", componentProps, {
    state,
    ref: forwardedRef,
    props: elementProps,
    stateAttributesMapping: collapsibleStateAttributesMapping
  });
  return /* @__PURE__ */ jsx(CollapsibleRootContext.Provider, {
    value: contextValue,
    children: element
  });
});
if (process.env.NODE_ENV !== "production") CollapsibleRoot.displayName = "CollapsibleRoot";
var stateAttributesMapping = __spreadValues(__spreadValues({}, triggerOpenStateMapping), transitionStatusMapping);
var CollapsibleTrigger = /* @__PURE__ */ React9.forwardRef(function CollapsibleTrigger2(componentProps, forwardedRef) {
  const {
    panelId,
    open,
    handleTrigger,
    state,
    disabled: contextDisabled
  } = useCollapsibleRootContext();
  const _a = componentProps, {
    className,
    disabled = contextDisabled,
    id,
    render,
    nativeButton = true
  } = _a, elementProps = __objRest(_a, [
    "className",
    "disabled",
    "id",
    "render",
    "nativeButton"
  ]);
  const {
    getButtonProps,
    buttonRef
  } = useButton({
    disabled,
    focusableWhenDisabled: true,
    native: nativeButton
  });
  const props = React9.useMemo(() => ({
    "aria-controls": open ? panelId : void 0,
    "aria-expanded": open,
    onClick: handleTrigger
  }), [panelId, open, handleTrigger]);
  const element = useRenderElement("button", componentProps, {
    state,
    ref: [forwardedRef, buttonRef],
    props: [props, elementProps, getButtonProps],
    stateAttributesMapping
  });
  return element;
});
if (process.env.NODE_ENV !== "production") CollapsibleTrigger.displayName = "CollapsibleTrigger";

// ../../node_modules/@base-ui/react/esm/collapsible/panel/CollapsiblePanelCssVars.js
var CollapsiblePanelCssVars = /* @__PURE__ */ (function(CollapsiblePanelCssVars2) {
  CollapsiblePanelCssVars2["collapsiblePanelHeight"] = "--collapsible-panel-height";
  CollapsiblePanelCssVars2["collapsiblePanelWidth"] = "--collapsible-panel-width";
  return CollapsiblePanelCssVars2;
})({});

// ../../node_modules/@base-ui/react/esm/collapsible/panel/CollapsiblePanel.js
var CollapsiblePanel = /* @__PURE__ */ React9.forwardRef(function CollapsiblePanel2(componentProps, forwardedRef) {
  const _a = componentProps, {
    className,
    hiddenUntilFound: hiddenUntilFoundProp,
    keepMounted: keepMountedProp,
    render,
    id: idProp
  } = _a, elementProps = __objRest(_a, [
    "className",
    "hiddenUntilFound",
    "keepMounted",
    "render",
    "id"
  ]);
  if (process.env.NODE_ENV !== "production") {
    useIsoLayoutEffect(() => {
      if (hiddenUntilFoundProp && keepMountedProp === false) {
        warn("The `keepMounted={false}` prop on a Collapsible will be ignored when using `hiddenUntilFound` since it requires the Panel to remain mounted even when closed.");
      }
    }, [hiddenUntilFoundProp, keepMountedProp]);
  }
  const {
    abortControllerRef,
    animationTypeRef,
    height,
    mounted,
    onOpenChange,
    open,
    panelId,
    panelRef,
    runOnceAnimationsFinish,
    setDimensions,
    setHiddenUntilFound,
    setKeepMounted,
    setMounted,
    setPanelIdState,
    setOpen,
    setVisible,
    state,
    transitionDimensionRef,
    visible,
    width,
    transitionStatus
  } = useCollapsibleRootContext();
  const hiddenUntilFound = hiddenUntilFoundProp != null ? hiddenUntilFoundProp : false;
  const keepMounted = keepMountedProp != null ? keepMountedProp : false;
  useIsoLayoutEffect(() => {
    if (idProp) {
      setPanelIdState(idProp);
      return () => {
        setPanelIdState(void 0);
      };
    }
    return void 0;
  }, [idProp, setPanelIdState]);
  useIsoLayoutEffect(() => {
    setHiddenUntilFound(hiddenUntilFound);
  }, [setHiddenUntilFound, hiddenUntilFound]);
  useIsoLayoutEffect(() => {
    setKeepMounted(keepMounted);
  }, [setKeepMounted, keepMounted]);
  const {
    props
  } = useCollapsiblePanel({
    abortControllerRef,
    animationTypeRef,
    externalRef: forwardedRef,
    height,
    hiddenUntilFound,
    id: panelId,
    keepMounted,
    mounted,
    onOpenChange,
    open,
    panelRef,
    runOnceAnimationsFinish,
    setDimensions,
    setMounted,
    setOpen,
    setVisible,
    transitionDimensionRef,
    visible,
    width
  });
  useOpenChangeComplete({
    open: open && transitionStatus === "idle",
    ref: panelRef,
    onComplete() {
      if (!open) {
        return;
      }
      setDimensions({
        height: void 0,
        width: void 0
      });
    }
  });
  const panelState = React9.useMemo(() => __spreadProps(__spreadValues({}, state), {
    transitionStatus
  }), [state, transitionStatus]);
  const element = useRenderElement("div", componentProps, {
    state: panelState,
    ref: [forwardedRef, panelRef],
    props: [props, {
      style: {
        [CollapsiblePanelCssVars.collapsiblePanelHeight]: height === void 0 ? "auto" : `${height}px`,
        [CollapsiblePanelCssVars.collapsiblePanelWidth]: width === void 0 ? "auto" : `${width}px`
      }
    }, elementProps],
    stateAttributesMapping: collapsibleStateAttributesMapping
  });
  const shouldRender = keepMounted || hiddenUntilFound || !keepMounted && mounted;
  if (!shouldRender) {
    return null;
  }
  return element;
});
if (process.env.NODE_ENV !== "production") CollapsiblePanel.displayName = "CollapsiblePanel";
function Collapsible(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports3.Root, __spreadValues({ "data-slot": "collapsible" }, props));
}
function CollapsibleTrigger3(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports3.Trigger, __spreadValues({ "data-slot": "collapsible-trigger" }, props));
}
function CollapsibleContent(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports3.Panel, __spreadValues({ "data-slot": "collapsible-content" }, props));
}

// ../../node_modules/@base-ui/react/esm/context-menu/index.parts.js
var index_parts_exports4 = {};
__export(index_parts_exports4, {
  Arrow: () => MenuArrow,
  Backdrop: () => MenuBackdrop,
  CheckboxItem: () => MenuCheckboxItem,
  CheckboxItemIndicator: () => MenuCheckboxItemIndicator,
  Group: () => MenuGroup,
  GroupLabel: () => MenuGroupLabel,
  Item: () => MenuItem,
  LinkItem: () => MenuLinkItem,
  Popup: () => MenuPopup,
  Portal: () => MenuPortal,
  Positioner: () => MenuPositioner,
  RadioGroup: () => MenuRadioGroup,
  RadioItem: () => MenuRadioItem,
  RadioItemIndicator: () => MenuRadioItemIndicator,
  Root: () => ContextMenuRoot,
  Separator: () => Separator,
  SubmenuRoot: () => MenuSubmenuRoot,
  SubmenuTrigger: () => MenuSubmenuTrigger,
  Trigger: () => ContextMenuTrigger
});
function ContextMenuRoot(props) {
  const [anchor, setAnchor] = React9.useState({
    getBoundingClientRect() {
      return DOMRect.fromRect({
        width: 0,
        height: 0,
        x: 0,
        y: 0
      });
    }
  });
  const backdropRef = React9.useRef(null);
  const internalBackdropRef = React9.useRef(null);
  const actionsRef = React9.useRef(null);
  const positionerRef = React9.useRef(null);
  const allowMouseUpTriggerRef = React9.useRef(true);
  const initialCursorPointRef = React9.useRef(null);
  const id = useId();
  const contextValue = React9.useMemo(() => ({
    anchor,
    setAnchor,
    actionsRef,
    backdropRef,
    internalBackdropRef,
    positionerRef,
    allowMouseUpTriggerRef,
    initialCursorPointRef,
    rootId: id
  }), [anchor, id]);
  return /* @__PURE__ */ jsx(ContextMenuRootContext.Provider, {
    value: contextValue,
    children: /* @__PURE__ */ jsx(MenuRootContext.Provider, {
      value: void 0,
      children: /* @__PURE__ */ jsx(index_parts_exports.Root, __spreadValues({}, props))
    })
  });
}
var LONG_PRESS_DELAY = 500;
var ContextMenuTrigger = /* @__PURE__ */ React9.forwardRef(function ContextMenuTrigger2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className"
  ]);
  const {
    setAnchor,
    actionsRef,
    internalBackdropRef,
    backdropRef,
    positionerRef,
    allowMouseUpTriggerRef,
    initialCursorPointRef,
    rootId
  } = useContextMenuRootContext(false);
  const {
    store
  } = useMenuRootContext(false);
  const open = store.useState("open");
  const disabled = store.useState("disabled");
  const triggerRef = React9.useRef(null);
  const touchPositionRef = React9.useRef(null);
  const longPressTimeout = useTimeout();
  const allowMouseUpTimeout = useTimeout();
  const allowMouseUpRef = React9.useRef(false);
  function handleLongPress(x, y, event) {
    var _a2;
    const isTouchEvent = event.type.startsWith("touch");
    initialCursorPointRef.current = {
      x,
      y
    };
    setAnchor({
      getBoundingClientRect() {
        return DOMRect.fromRect({
          width: isTouchEvent ? 10 : 0,
          height: isTouchEvent ? 10 : 0,
          x,
          y
        });
      }
    });
    allowMouseUpRef.current = false;
    (_a2 = actionsRef.current) == null ? void 0 : _a2.setOpen(true, createChangeEventDetails(reason_parts_exports.triggerPress, event));
    allowMouseUpTimeout.start(LONG_PRESS_DELAY, () => {
      allowMouseUpRef.current = true;
    });
  }
  function handleContextMenu(event) {
    if (disabled) {
      return;
    }
    allowMouseUpTriggerRef.current = true;
    stopEvent(event);
    handleLongPress(event.clientX, event.clientY, event.nativeEvent);
    const doc = ownerDocument(triggerRef.current);
    doc.addEventListener("mouseup", (mouseEvent) => {
      var _a2;
      allowMouseUpTriggerRef.current = false;
      if (!allowMouseUpRef.current) {
        return;
      }
      allowMouseUpTimeout.clear();
      allowMouseUpRef.current = false;
      const mouseUpTarget = getTarget(mouseEvent);
      if (contains(positionerRef.current, mouseUpTarget)) {
        return;
      }
      if (rootId && mouseUpTarget && findRootOwnerId(mouseUpTarget) === rootId) {
        return;
      }
      (_a2 = actionsRef.current) == null ? void 0 : _a2.setOpen(false, createChangeEventDetails(reason_parts_exports.cancelOpen, mouseEvent));
    }, {
      once: true
    });
  }
  function handleTouchStart(event) {
    if (disabled) {
      return;
    }
    allowMouseUpTriggerRef.current = false;
    if (event.touches.length === 1) {
      event.stopPropagation();
      const touch = event.touches[0];
      touchPositionRef.current = {
        x: touch.clientX,
        y: touch.clientY
      };
      longPressTimeout.start(LONG_PRESS_DELAY, () => {
        if (touchPositionRef.current) {
          handleLongPress(touchPositionRef.current.x, touchPositionRef.current.y, event.nativeEvent);
        }
      });
    }
  }
  function handleTouchMove(event) {
    if (longPressTimeout.isStarted() && touchPositionRef.current && event.touches.length === 1) {
      const touch = event.touches[0];
      const moveThreshold = 10;
      const deltaX = Math.abs(touch.clientX - touchPositionRef.current.x);
      const deltaY = Math.abs(touch.clientY - touchPositionRef.current.y);
      if (deltaX > moveThreshold || deltaY > moveThreshold) {
        longPressTimeout.clear();
      }
    }
  }
  function handleTouchEnd() {
    longPressTimeout.clear();
    touchPositionRef.current = null;
  }
  React9.useEffect(() => {
    function handleDocumentContextMenu(event) {
      if (disabled) {
        return;
      }
      const target = getTarget(event);
      const targetElement = target;
      if (contains(triggerRef.current, targetElement) || contains(internalBackdropRef.current, targetElement) || contains(backdropRef.current, targetElement)) {
        event.preventDefault();
      }
    }
    const doc = ownerDocument(triggerRef.current);
    doc.addEventListener("contextmenu", handleDocumentContextMenu);
    return () => {
      doc.removeEventListener("contextmenu", handleDocumentContextMenu);
    };
  }, [backdropRef, disabled, internalBackdropRef]);
  const state = {
    open
  };
  const element = useRenderElement("div", componentProps, {
    state,
    ref: [triggerRef, forwardedRef],
    props: [{
      onContextMenu: handleContextMenu,
      onTouchStart: handleTouchStart,
      onTouchMove: handleTouchMove,
      onTouchEnd: handleTouchEnd,
      onTouchCancel: handleTouchEnd,
      style: {
        WebkitTouchCallout: "none"
      }
    }, elementProps],
    stateAttributesMapping: pressableTriggerOpenStateMapping
  });
  return element;
});
if (process.env.NODE_ENV !== "production") ContextMenuTrigger.displayName = "ContextMenuTrigger";
var menubarStateAttributesMapping = {
  hasSubmenuOpen(value) {
    return {
      "data-has-submenu-open": value ? "true" : "false"
    };
  }
};
var Menubar = /* @__PURE__ */ React9.forwardRef(function Menubar2(props, forwardedRef) {
  const _a = props, {
    orientation = "horizontal",
    loopFocus = true,
    render,
    className,
    modal = true,
    disabled = false,
    id: idProp
  } = _a, elementProps = __objRest(_a, [
    "orientation",
    "loopFocus",
    "render",
    "className",
    "modal",
    "disabled",
    "id"
  ]);
  const [contentElement, setContentElement] = React9.useState(null);
  const [hasSubmenuOpen, setHasSubmenuOpen] = React9.useState(false);
  const {
    openMethod,
    triggerProps: interactionTypeProps
  } = useOpenInteractionType(hasSubmenuOpen);
  useScrollLock(modal && hasSubmenuOpen && openMethod !== "touch", contentElement);
  const id = useBaseUiId(idProp);
  const state = {
    orientation,
    modal,
    hasSubmenuOpen
  };
  const contentRef = React9.useRef(null);
  const allowMouseUpTriggerRef = React9.useRef(false);
  const context = React9.useMemo(() => ({
    contentElement,
    setContentElement,
    setHasSubmenuOpen,
    hasSubmenuOpen,
    modal,
    disabled,
    orientation,
    allowMouseUpTriggerRef,
    rootId: id
  }), [contentElement, hasSubmenuOpen, modal, disabled, orientation, id]);
  return /* @__PURE__ */ jsx(MenubarContext.Provider, {
    value: context,
    children: /* @__PURE__ */ jsx(FloatingTree, {
      children: /* @__PURE__ */ jsx(MenubarContent, {
        children: /* @__PURE__ */ jsx(CompositeRoot, {
          render,
          className,
          state,
          stateAttributesMapping: menubarStateAttributesMapping,
          refs: [forwardedRef, setContentElement, contentRef],
          props: [{
            role: "menubar",
            id
          }, interactionTypeProps, elementProps],
          orientation,
          loopFocus,
          highlightItemOnHover: hasSubmenuOpen
        })
      })
    })
  });
});
if (process.env.NODE_ENV !== "production") Menubar.displayName = "Menubar";
function MenubarContent(props) {
  const nodeId = useFloatingNodeId();
  const {
    events: menuEvents
  } = useFloatingTree();
  const rootContext = useMenubarContext();
  React9.useEffect(() => {
    function onSubmenuOpenChange(details) {
      if (!details.nodeId || details.parentNodeId !== nodeId) {
        return;
      }
      if (details.open) {
        if (!rootContext.hasSubmenuOpen) {
          rootContext.setHasSubmenuOpen(true);
        }
      } else if (details.reason !== "sibling-open" && details.reason !== "list-navigation") {
        rootContext.setHasSubmenuOpen(false);
      }
    }
    menuEvents.on("menuopenchange", onSubmenuOpenChange);
    return () => {
      menuEvents.off("menuopenchange", onSubmenuOpenChange);
    };
  }, [menuEvents, nodeId, rootContext]);
  return /* @__PURE__ */ jsx(FloatingNode, {
    id: nodeId,
    children: props.children
  });
}

// ../../node_modules/@base-ui/react/esm/scroll-area/index.parts.js
var index_parts_exports5 = {};
__export(index_parts_exports5, {
  Content: () => ScrollAreaContent,
  Corner: () => ScrollAreaCorner,
  Root: () => ScrollAreaRoot,
  Scrollbar: () => ScrollAreaScrollbar,
  Thumb: () => ScrollAreaThumb,
  Viewport: () => ScrollAreaViewport
});
var ScrollAreaRootContext = /* @__PURE__ */ React9.createContext(void 0);
if (process.env.NODE_ENV !== "production") ScrollAreaRootContext.displayName = "ScrollAreaRootContext";
function useScrollAreaRootContext() {
  const context = React9.useContext(ScrollAreaRootContext);
  if (context === void 0) {
    throw new Error(process.env.NODE_ENV !== "production" ? "Base UI: ScrollAreaRootContext is missing. ScrollArea parts must be placed within <ScrollArea.Root>." : formatErrorMessage_default(53));
  }
  return context;
}

// ../../node_modules/@base-ui/react/esm/scroll-area/root/ScrollAreaRootCssVars.js
var ScrollAreaRootCssVars = /* @__PURE__ */ (function(ScrollAreaRootCssVars2) {
  ScrollAreaRootCssVars2["scrollAreaCornerHeight"] = "--scroll-area-corner-height";
  ScrollAreaRootCssVars2["scrollAreaCornerWidth"] = "--scroll-area-corner-width";
  return ScrollAreaRootCssVars2;
})({});

// ../../node_modules/@base-ui/react/esm/scroll-area/constants.js
var SCROLL_TIMEOUT = 500;
var MIN_THUMB_SIZE = 16;

// ../../node_modules/@base-ui/react/esm/scroll-area/utils/getOffset.js
function getOffset(element, prop, axis) {
  if (!element) {
    return 0;
  }
  const styles = getComputedStyle(element);
  const propAxis = axis === "x" ? "Inline" : "Block";
  if (axis === "x" && prop === "margin") {
    return parseFloat(styles[`${prop}InlineStart`]) * 2;
  }
  return parseFloat(styles[`${prop}${propAxis}Start`]) + parseFloat(styles[`${prop}${propAxis}End`]);
}

// ../../node_modules/@base-ui/react/esm/scroll-area/scrollbar/ScrollAreaScrollbarDataAttributes.js
var ScrollAreaScrollbarDataAttributes = /* @__PURE__ */ (function(ScrollAreaScrollbarDataAttributes2) {
  ScrollAreaScrollbarDataAttributes2["orientation"] = "data-orientation";
  ScrollAreaScrollbarDataAttributes2["hovering"] = "data-hovering";
  ScrollAreaScrollbarDataAttributes2["scrolling"] = "data-scrolling";
  ScrollAreaScrollbarDataAttributes2["hasOverflowX"] = "data-has-overflow-x";
  ScrollAreaScrollbarDataAttributes2["hasOverflowY"] = "data-has-overflow-y";
  ScrollAreaScrollbarDataAttributes2["overflowXStart"] = "data-overflow-x-start";
  ScrollAreaScrollbarDataAttributes2["overflowXEnd"] = "data-overflow-x-end";
  ScrollAreaScrollbarDataAttributes2["overflowYStart"] = "data-overflow-y-start";
  ScrollAreaScrollbarDataAttributes2["overflowYEnd"] = "data-overflow-y-end";
  return ScrollAreaScrollbarDataAttributes2;
})({});

// ../../node_modules/@base-ui/react/esm/scroll-area/root/ScrollAreaRootDataAttributes.js
var ScrollAreaRootDataAttributes = /* @__PURE__ */ (function(ScrollAreaRootDataAttributes2) {
  ScrollAreaRootDataAttributes2["scrolling"] = "data-scrolling";
  ScrollAreaRootDataAttributes2["hasOverflowX"] = "data-has-overflow-x";
  ScrollAreaRootDataAttributes2["hasOverflowY"] = "data-has-overflow-y";
  ScrollAreaRootDataAttributes2["overflowXStart"] = "data-overflow-x-start";
  ScrollAreaRootDataAttributes2["overflowXEnd"] = "data-overflow-x-end";
  ScrollAreaRootDataAttributes2["overflowYStart"] = "data-overflow-y-start";
  ScrollAreaRootDataAttributes2["overflowYEnd"] = "data-overflow-y-end";
  return ScrollAreaRootDataAttributes2;
})({});

// ../../node_modules/@base-ui/react/esm/scroll-area/root/stateAttributes.js
var scrollAreaStateAttributesMapping = {
  hasOverflowX: (value) => value ? {
    [ScrollAreaRootDataAttributes.hasOverflowX]: ""
  } : null,
  hasOverflowY: (value) => value ? {
    [ScrollAreaRootDataAttributes.hasOverflowY]: ""
  } : null,
  overflowXStart: (value) => value ? {
    [ScrollAreaRootDataAttributes.overflowXStart]: ""
  } : null,
  overflowXEnd: (value) => value ? {
    [ScrollAreaRootDataAttributes.overflowXEnd]: ""
  } : null,
  overflowYStart: (value) => value ? {
    [ScrollAreaRootDataAttributes.overflowYStart]: ""
  } : null,
  overflowYEnd: (value) => value ? {
    [ScrollAreaRootDataAttributes.overflowYEnd]: ""
  } : null,
  cornerHidden: () => null
};
var DEFAULT_COORDS = {
  x: 0,
  y: 0
};
var DEFAULT_SIZE = {
  width: 0,
  height: 0
};
var DEFAULT_OVERFLOW_EDGES = {
  xStart: false,
  xEnd: false,
  yStart: false,
  yEnd: false
};
var DEFAULT_HIDDEN_STATE = {
  x: true,
  y: true,
  corner: true
};
var ScrollAreaRoot = /* @__PURE__ */ React9.forwardRef(function ScrollAreaRoot2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className,
    overflowEdgeThreshold: overflowEdgeThresholdProp
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className",
    "overflowEdgeThreshold"
  ]);
  const overflowEdgeThreshold = normalizeOverflowEdgeThreshold(overflowEdgeThresholdProp);
  const rootId = useBaseUiId();
  const scrollYTimeout = useTimeout();
  const scrollXTimeout = useTimeout();
  const {
    nonce,
    disableStyleElements
  } = useCSPContext();
  const [hovering, setHovering] = React9.useState(false);
  const [scrollingX, setScrollingX] = React9.useState(false);
  const [scrollingY, setScrollingY] = React9.useState(false);
  const [touchModality, setTouchModality] = React9.useState(false);
  const [hasMeasuredScrollbar, setHasMeasuredScrollbar] = React9.useState(false);
  const [cornerSize, setCornerSize] = React9.useState(DEFAULT_SIZE);
  const [thumbSize, setThumbSize] = React9.useState(DEFAULT_SIZE);
  const [overflowEdges, setOverflowEdges] = React9.useState(DEFAULT_OVERFLOW_EDGES);
  const [hiddenState, setHiddenState] = React9.useState(DEFAULT_HIDDEN_STATE);
  const rootRef = React9.useRef(null);
  const viewportRef = React9.useRef(null);
  const scrollbarYRef = React9.useRef(null);
  const scrollbarXRef = React9.useRef(null);
  const thumbYRef = React9.useRef(null);
  const thumbXRef = React9.useRef(null);
  const cornerRef = React9.useRef(null);
  const thumbDraggingRef = React9.useRef(false);
  const startYRef = React9.useRef(0);
  const startXRef = React9.useRef(0);
  const startScrollTopRef = React9.useRef(0);
  const startScrollLeftRef = React9.useRef(0);
  const currentOrientationRef = React9.useRef("vertical");
  const scrollPositionRef = React9.useRef(DEFAULT_COORDS);
  const handleScroll = useStableCallback((scrollPosition) => {
    const offsetX = scrollPosition.x - scrollPositionRef.current.x;
    const offsetY = scrollPosition.y - scrollPositionRef.current.y;
    scrollPositionRef.current = scrollPosition;
    if (offsetY !== 0) {
      setScrollingY(true);
      scrollYTimeout.start(SCROLL_TIMEOUT, () => {
        setScrollingY(false);
      });
    }
    if (offsetX !== 0) {
      setScrollingX(true);
      scrollXTimeout.start(SCROLL_TIMEOUT, () => {
        setScrollingX(false);
      });
    }
  });
  const handlePointerDown = useStableCallback((event) => {
    if (event.button !== 0) {
      return;
    }
    thumbDraggingRef.current = true;
    startYRef.current = event.clientY;
    startXRef.current = event.clientX;
    currentOrientationRef.current = event.currentTarget.getAttribute(ScrollAreaScrollbarDataAttributes.orientation);
    if (viewportRef.current) {
      startScrollTopRef.current = viewportRef.current.scrollTop;
      startScrollLeftRef.current = viewportRef.current.scrollLeft;
    }
    if (thumbYRef.current && currentOrientationRef.current === "vertical") {
      thumbYRef.current.setPointerCapture(event.pointerId);
    }
    if (thumbXRef.current && currentOrientationRef.current === "horizontal") {
      thumbXRef.current.setPointerCapture(event.pointerId);
    }
  });
  const handlePointerMove = useStableCallback((event) => {
    if (!thumbDraggingRef.current) {
      return;
    }
    const deltaY = event.clientY - startYRef.current;
    const deltaX = event.clientX - startXRef.current;
    if (viewportRef.current) {
      const scrollableContentHeight = viewportRef.current.scrollHeight;
      const viewportHeight = viewportRef.current.clientHeight;
      const scrollableContentWidth = viewportRef.current.scrollWidth;
      const viewportWidth = viewportRef.current.clientWidth;
      if (thumbYRef.current && scrollbarYRef.current && currentOrientationRef.current === "vertical") {
        const scrollbarYOffset = getOffset(scrollbarYRef.current, "padding", "y");
        const thumbYOffset = getOffset(thumbYRef.current, "margin", "y");
        const thumbHeight = thumbYRef.current.offsetHeight;
        const maxThumbOffsetY = scrollbarYRef.current.offsetHeight - thumbHeight - scrollbarYOffset - thumbYOffset;
        const scrollRatioY = deltaY / maxThumbOffsetY;
        viewportRef.current.scrollTop = startScrollTopRef.current + scrollRatioY * (scrollableContentHeight - viewportHeight);
        event.preventDefault();
        setScrollingY(true);
        scrollYTimeout.start(SCROLL_TIMEOUT, () => {
          setScrollingY(false);
        });
      }
      if (thumbXRef.current && scrollbarXRef.current && currentOrientationRef.current === "horizontal") {
        const scrollbarXOffset = getOffset(scrollbarXRef.current, "padding", "x");
        const thumbXOffset = getOffset(thumbXRef.current, "margin", "x");
        const thumbWidth = thumbXRef.current.offsetWidth;
        const maxThumbOffsetX = scrollbarXRef.current.offsetWidth - thumbWidth - scrollbarXOffset - thumbXOffset;
        const scrollRatioX = deltaX / maxThumbOffsetX;
        viewportRef.current.scrollLeft = startScrollLeftRef.current + scrollRatioX * (scrollableContentWidth - viewportWidth);
        event.preventDefault();
        setScrollingX(true);
        scrollXTimeout.start(SCROLL_TIMEOUT, () => {
          setScrollingX(false);
        });
      }
    }
  });
  const handlePointerUp = useStableCallback((event) => {
    thumbDraggingRef.current = false;
    if (thumbYRef.current && currentOrientationRef.current === "vertical") {
      thumbYRef.current.releasePointerCapture(event.pointerId);
    }
    if (thumbXRef.current && currentOrientationRef.current === "horizontal") {
      thumbXRef.current.releasePointerCapture(event.pointerId);
    }
  });
  function handleTouchModalityChange(event) {
    setTouchModality(event.pointerType === "touch");
  }
  function handlePointerEnterOrMove(event) {
    handleTouchModalityChange(event);
    if (event.pointerType !== "touch") {
      const isTargetRootChild = contains(rootRef.current, event.target);
      setHovering(isTargetRootChild);
    }
  }
  const state = React9.useMemo(() => ({
    scrolling: scrollingX || scrollingY,
    hasOverflowX: !hiddenState.x,
    hasOverflowY: !hiddenState.y,
    overflowXStart: overflowEdges.xStart,
    overflowXEnd: overflowEdges.xEnd,
    overflowYStart: overflowEdges.yStart,
    overflowYEnd: overflowEdges.yEnd,
    cornerHidden: hiddenState.corner
  }), [scrollingX, scrollingY, hiddenState.x, hiddenState.y, hiddenState.corner, overflowEdges]);
  const props = {
    role: "presentation",
    onPointerEnter: handlePointerEnterOrMove,
    onPointerMove: handlePointerEnterOrMove,
    onPointerDown: handleTouchModalityChange,
    onPointerLeave() {
      setHovering(false);
    },
    style: {
      position: "relative",
      [ScrollAreaRootCssVars.scrollAreaCornerHeight]: `${cornerSize.height}px`,
      [ScrollAreaRootCssVars.scrollAreaCornerWidth]: `${cornerSize.width}px`
    }
  };
  const element = useRenderElement("div", componentProps, {
    state,
    ref: [forwardedRef, rootRef],
    props: [props, elementProps],
    stateAttributesMapping: scrollAreaStateAttributesMapping
  });
  const contextValue = React9.useMemo(() => ({
    handlePointerDown,
    handlePointerMove,
    handlePointerUp,
    handleScroll,
    cornerSize,
    setCornerSize,
    thumbSize,
    setThumbSize,
    hasMeasuredScrollbar,
    setHasMeasuredScrollbar,
    touchModality,
    cornerRef,
    scrollingX,
    setScrollingX,
    scrollingY,
    setScrollingY,
    hovering,
    setHovering,
    viewportRef,
    rootRef,
    scrollbarYRef,
    scrollbarXRef,
    thumbYRef,
    thumbXRef,
    rootId,
    hiddenState,
    setHiddenState,
    overflowEdges,
    setOverflowEdges,
    viewportState: state,
    overflowEdgeThreshold
  }), [handlePointerDown, handlePointerMove, handlePointerUp, handleScroll, cornerSize, thumbSize, hasMeasuredScrollbar, touchModality, scrollingX, setScrollingX, scrollingY, setScrollingY, hovering, setHovering, rootId, hiddenState, overflowEdges, state, overflowEdgeThreshold]);
  return /* @__PURE__ */ jsxs(ScrollAreaRootContext.Provider, {
    value: contextValue,
    children: [!disableStyleElements && styleDisableScrollbar.getElement(nonce), element]
  });
});
if (process.env.NODE_ENV !== "production") ScrollAreaRoot.displayName = "ScrollAreaRoot";
function normalizeOverflowEdgeThreshold(threshold) {
  if (typeof threshold === "number") {
    const value = Math.max(0, threshold);
    return {
      xStart: value,
      xEnd: value,
      yStart: value,
      yEnd: value
    };
  }
  return {
    xStart: Math.max(0, (threshold == null ? void 0 : threshold.xStart) || 0),
    xEnd: Math.max(0, (threshold == null ? void 0 : threshold.xEnd) || 0),
    yStart: Math.max(0, (threshold == null ? void 0 : threshold.yStart) || 0),
    yEnd: Math.max(0, (threshold == null ? void 0 : threshold.yEnd) || 0)
  };
}
var ScrollAreaViewportContext = /* @__PURE__ */ React9.createContext(void 0);
if (process.env.NODE_ENV !== "production") ScrollAreaViewportContext.displayName = "ScrollAreaViewportContext";
function useScrollAreaViewportContext() {
  const context = React9.useContext(ScrollAreaViewportContext);
  if (context === void 0) {
    throw new Error(process.env.NODE_ENV !== "production" ? "Base UI: ScrollAreaViewportContext missing. ScrollAreaViewport parts must be placed within <ScrollArea.Viewport>." : formatErrorMessage_default(55));
  }
  return context;
}

// ../../node_modules/@base-ui/react/esm/scroll-area/utils/onVisible.js
function onVisible(element, callback) {
  if (typeof IntersectionObserver === "undefined") {
    return () => {
    };
  }
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.intersectionRatio > 0) {
        callback();
        observer.disconnect();
      }
    });
  });
  observer.observe(element);
  return () => {
    observer.disconnect();
  };
}

// ../../node_modules/@base-ui/react/esm/scroll-area/viewport/ScrollAreaViewportCssVars.js
var ScrollAreaViewportCssVars = /* @__PURE__ */ (function(ScrollAreaViewportCssVars2) {
  ScrollAreaViewportCssVars2["scrollAreaOverflowXStart"] = "--scroll-area-overflow-x-start";
  ScrollAreaViewportCssVars2["scrollAreaOverflowXEnd"] = "--scroll-area-overflow-x-end";
  ScrollAreaViewportCssVars2["scrollAreaOverflowYStart"] = "--scroll-area-overflow-y-start";
  ScrollAreaViewportCssVars2["scrollAreaOverflowYEnd"] = "--scroll-area-overflow-y-end";
  return ScrollAreaViewportCssVars2;
})({});

// ../../node_modules/@base-ui/react/esm/scroll-area/utils/scrollEdges.js
var SCROLL_EDGE_TOLERANCE_PX = 1;
function normalizeScrollOffset(value, max) {
  if (max <= 0) {
    return 0;
  }
  const clamped = clamp(value, 0, max);
  const startDistance = clamped;
  const endDistance = max - clamped;
  const withinStartTolerance = startDistance <= SCROLL_EDGE_TOLERANCE_PX;
  const withinEndTolerance = endDistance <= SCROLL_EDGE_TOLERANCE_PX;
  if (withinStartTolerance && withinEndTolerance) {
    return startDistance <= endDistance ? 0 : max;
  }
  if (withinStartTolerance) {
    return 0;
  }
  if (withinEndTolerance) {
    return max;
  }
  return clamped;
}
var scrollAreaOverflowVarsRegistered = false;
var HAS_RESIZE_OBSERVER = typeof ResizeObserver !== "undefined";
function removeCSSVariableInheritance() {
  if (scrollAreaOverflowVarsRegistered || // When `inherits: false`, specifying `inherit` on child elements doesn't work
  // in Safari. To let CSS features work correctly, this optimization must be skipped.
  isWebKit) {
    return;
  }
  if (typeof CSS !== "undefined" && "registerProperty" in CSS) {
    [ScrollAreaViewportCssVars.scrollAreaOverflowXStart, ScrollAreaViewportCssVars.scrollAreaOverflowXEnd, ScrollAreaViewportCssVars.scrollAreaOverflowYStart, ScrollAreaViewportCssVars.scrollAreaOverflowYEnd].forEach((name) => {
      try {
        CSS.registerProperty({
          name,
          syntax: "<length>",
          inherits: false,
          initialValue: "0px"
        });
      } catch (e) {
      }
    });
  }
  scrollAreaOverflowVarsRegistered = true;
}
var ScrollAreaViewport = /* @__PURE__ */ React9.forwardRef(function ScrollAreaViewport2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className"
  ]);
  const {
    viewportRef,
    scrollbarYRef,
    scrollbarXRef,
    thumbYRef,
    thumbXRef,
    cornerRef,
    cornerSize,
    setCornerSize,
    setThumbSize,
    rootId,
    setHiddenState,
    hiddenState,
    setHasMeasuredScrollbar,
    handleScroll,
    setHovering,
    setOverflowEdges,
    overflowEdges,
    overflowEdgeThreshold,
    scrollingX,
    scrollingY
  } = useScrollAreaRootContext();
  const direction = useDirection();
  const programmaticScrollRef = React9.useRef(true);
  const lastMeasuredViewportMetricsRef = React9.useRef([NaN, NaN, NaN, NaN]);
  const scrollEndTimeout = useTimeout();
  const waitForAnimationsTimeout = useTimeout();
  const computeThumbPosition = useStableCallback(() => {
    const viewportEl = viewportRef.current;
    const scrollbarYEl = scrollbarYRef.current;
    const scrollbarXEl = scrollbarXRef.current;
    const thumbYEl = thumbYRef.current;
    const thumbXEl = thumbXRef.current;
    const cornerEl = cornerRef.current;
    if (!viewportEl) {
      return;
    }
    const scrollableContentHeight = viewportEl.scrollHeight;
    const scrollableContentWidth = viewportEl.scrollWidth;
    const viewportHeight = viewportEl.clientHeight;
    const viewportWidth = viewportEl.clientWidth;
    const scrollTop = viewportEl.scrollTop;
    const scrollLeft = viewportEl.scrollLeft;
    const lastMeasuredViewportMetrics = lastMeasuredViewportMetricsRef.current;
    const isFirstMeasurement = Number.isNaN(lastMeasuredViewportMetrics[0]);
    lastMeasuredViewportMetrics[0] = viewportHeight;
    lastMeasuredViewportMetrics[1] = scrollableContentHeight;
    lastMeasuredViewportMetrics[2] = viewportWidth;
    lastMeasuredViewportMetrics[3] = scrollableContentWidth;
    if (isFirstMeasurement) {
      setHasMeasuredScrollbar(true);
    }
    if (scrollableContentHeight === 0 || scrollableContentWidth === 0) {
      return;
    }
    const nextHiddenState = getHiddenState(viewportEl);
    const scrollbarYHidden = nextHiddenState.y;
    const scrollbarXHidden = nextHiddenState.x;
    const ratioX = viewportWidth / scrollableContentWidth;
    const ratioY = viewportHeight / scrollableContentHeight;
    const maxScrollLeft = Math.max(0, scrollableContentWidth - viewportWidth);
    const maxScrollTop = Math.max(0, scrollableContentHeight - viewportHeight);
    let scrollLeftFromStart = 0;
    let scrollLeftFromEnd = 0;
    if (!scrollbarXHidden) {
      let rawScrollLeftFromStart = 0;
      if (direction === "rtl") {
        rawScrollLeftFromStart = clamp(-scrollLeft, 0, maxScrollLeft);
      } else {
        rawScrollLeftFromStart = clamp(scrollLeft, 0, maxScrollLeft);
      }
      scrollLeftFromStart = normalizeScrollOffset(rawScrollLeftFromStart, maxScrollLeft);
      scrollLeftFromEnd = maxScrollLeft - scrollLeftFromStart;
    }
    const rawScrollTopFromStart = !scrollbarYHidden ? clamp(scrollTop, 0, maxScrollTop) : 0;
    const scrollTopFromStart = !scrollbarYHidden ? normalizeScrollOffset(rawScrollTopFromStart, maxScrollTop) : 0;
    const scrollTopFromEnd = !scrollbarYHidden ? maxScrollTop - scrollTopFromStart : 0;
    const nextWidth = scrollbarXHidden ? 0 : viewportWidth;
    const nextHeight = scrollbarYHidden ? 0 : viewportHeight;
    let nextCornerWidth = 0;
    let nextCornerHeight = 0;
    if (!scrollbarXHidden && !scrollbarYHidden) {
      nextCornerWidth = (scrollbarYEl == null ? void 0 : scrollbarYEl.offsetWidth) || 0;
      nextCornerHeight = (scrollbarXEl == null ? void 0 : scrollbarXEl.offsetHeight) || 0;
    }
    const cornerNotYetSized = cornerSize.width === 0 && cornerSize.height === 0;
    const cornerWidthOffset = cornerNotYetSized ? nextCornerWidth : 0;
    const cornerHeightOffset = cornerNotYetSized ? nextCornerHeight : 0;
    const scrollbarXOffset = getOffset(scrollbarXEl, "padding", "x");
    const scrollbarYOffset = getOffset(scrollbarYEl, "padding", "y");
    const thumbXOffset = getOffset(thumbXEl, "margin", "x");
    const thumbYOffset = getOffset(thumbYEl, "margin", "y");
    const idealNextWidth = nextWidth - scrollbarXOffset - thumbXOffset;
    const idealNextHeight = nextHeight - scrollbarYOffset - thumbYOffset;
    const maxNextWidth = scrollbarXEl ? Math.min(scrollbarXEl.offsetWidth - cornerWidthOffset, idealNextWidth) : idealNextWidth;
    const maxNextHeight = scrollbarYEl ? Math.min(scrollbarYEl.offsetHeight - cornerHeightOffset, idealNextHeight) : idealNextHeight;
    const clampedNextWidth = Math.max(MIN_THUMB_SIZE, maxNextWidth * ratioX);
    const clampedNextHeight = Math.max(MIN_THUMB_SIZE, maxNextHeight * ratioY);
    setThumbSize((prevSize) => {
      if (prevSize.height === clampedNextHeight && prevSize.width === clampedNextWidth) {
        return prevSize;
      }
      return {
        width: clampedNextWidth,
        height: clampedNextHeight
      };
    });
    if (scrollbarYEl && thumbYEl) {
      const maxThumbOffsetY = scrollbarYEl.offsetHeight - clampedNextHeight - scrollbarYOffset - thumbYOffset;
      const scrollRangeY = scrollableContentHeight - viewportHeight;
      const scrollRatioY = scrollRangeY === 0 ? 0 : scrollTop / scrollRangeY;
      const thumbOffsetY = Math.min(maxThumbOffsetY, Math.max(0, scrollRatioY * maxThumbOffsetY));
      thumbYEl.style.transform = `translate3d(0,${thumbOffsetY}px,0)`;
    }
    if (scrollbarXEl && thumbXEl) {
      const maxThumbOffsetX = scrollbarXEl.offsetWidth - clampedNextWidth - scrollbarXOffset - thumbXOffset;
      const scrollRangeX = scrollableContentWidth - viewportWidth;
      const scrollRatioX = scrollRangeX === 0 ? 0 : scrollLeft / scrollRangeX;
      const thumbOffsetX = direction === "rtl" ? clamp(scrollRatioX * maxThumbOffsetX, -maxThumbOffsetX, 0) : clamp(scrollRatioX * maxThumbOffsetX, 0, maxThumbOffsetX);
      thumbXEl.style.transform = `translate3d(${thumbOffsetX}px,0,0)`;
    }
    const overflowMetricsPx = [[ScrollAreaViewportCssVars.scrollAreaOverflowXStart, scrollLeftFromStart], [ScrollAreaViewportCssVars.scrollAreaOverflowXEnd, scrollLeftFromEnd], [ScrollAreaViewportCssVars.scrollAreaOverflowYStart, scrollTopFromStart], [ScrollAreaViewportCssVars.scrollAreaOverflowYEnd, scrollTopFromEnd]];
    for (const [cssVar, value] of overflowMetricsPx) {
      viewportEl.style.setProperty(cssVar, `${value}px`);
    }
    if (cornerEl) {
      if (scrollbarXHidden || scrollbarYHidden) {
        setCornerSize({
          width: 0,
          height: 0
        });
      } else if (!scrollbarXHidden && !scrollbarYHidden) {
        setCornerSize({
          width: nextCornerWidth,
          height: nextCornerHeight
        });
      }
    }
    setHiddenState((prevState) => mergeHiddenState(prevState, nextHiddenState));
    const nextOverflowEdges = {
      xStart: !scrollbarXHidden && scrollLeftFromStart > overflowEdgeThreshold.xStart,
      xEnd: !scrollbarXHidden && scrollLeftFromEnd > overflowEdgeThreshold.xEnd,
      yStart: !scrollbarYHidden && scrollTopFromStart > overflowEdgeThreshold.yStart,
      yEnd: !scrollbarYHidden && scrollTopFromEnd > overflowEdgeThreshold.yEnd
    };
    setOverflowEdges((prev) => {
      if (prev.xStart === nextOverflowEdges.xStart && prev.xEnd === nextOverflowEdges.xEnd && prev.yStart === nextOverflowEdges.yStart && prev.yEnd === nextOverflowEdges.yEnd) {
        return prev;
      }
      return nextOverflowEdges;
    });
  });
  useIsoLayoutEffect(() => {
    if (!viewportRef.current) {
      return void 0;
    }
    removeCSSVariableInheritance();
    if (HAS_RESIZE_OBSERVER) {
      return void 0;
    }
    return onVisible(viewportRef.current, () => {
      computeThumbPosition();
    });
  }, [computeThumbPosition, viewportRef]);
  useIsoLayoutEffect(() => {
    queueMicrotask(computeThumbPosition);
  }, [computeThumbPosition, hiddenState, direction]);
  useIsoLayoutEffect(() => {
    var _a2;
    if ((_a2 = viewportRef.current) == null ? void 0 : _a2.matches(":hover")) {
      setHovering(true);
    }
  }, [viewportRef, setHovering]);
  React9.useEffect(() => {
    const viewport = viewportRef.current;
    if (!HAS_RESIZE_OBSERVER || !viewport) {
      return void 0;
    }
    let hasInitialized = false;
    const ro = new ResizeObserver(() => {
      if (!hasInitialized) {
        hasInitialized = true;
        const lastMeasuredViewportMetrics = lastMeasuredViewportMetricsRef.current;
        if (lastMeasuredViewportMetrics[0] === viewport.clientHeight && lastMeasuredViewportMetrics[1] === viewport.scrollHeight && lastMeasuredViewportMetrics[2] === viewport.clientWidth && lastMeasuredViewportMetrics[3] === viewport.scrollWidth) {
          return;
        }
      }
      computeThumbPosition();
    });
    ro.observe(viewport);
    waitForAnimationsTimeout.start(0, () => {
      const animations = viewport.getAnimations({
        subtree: true
      });
      if (animations.length === 0) {
        return;
      }
      Promise.allSettled(animations.map((animation) => animation.finished)).then(computeThumbPosition).catch(() => {
      });
    });
    return () => {
      ro.disconnect();
      waitForAnimationsTimeout.clear();
    };
  }, [computeThumbPosition, viewportRef, waitForAnimationsTimeout]);
  function handleUserInteraction() {
    programmaticScrollRef.current = false;
  }
  const props = __spreadProps(__spreadValues({
    role: "presentation"
  }, rootId && {
    "data-id": `${rootId}-viewport`
  }), {
    // https://accessibilityinsights.io/info-examples/web/scrollable-region-focusable/
    // Keep non-scrollable viewports out of tab order.
    tabIndex: hiddenState.x && hiddenState.y ? -1 : 0,
    className: styleDisableScrollbar.className,
    style: {
      overflow: "scroll"
    },
    onScroll() {
      if (!viewportRef.current) {
        return;
      }
      computeThumbPosition();
      if (!programmaticScrollRef.current) {
        handleScroll({
          x: viewportRef.current.scrollLeft,
          y: viewportRef.current.scrollTop
        });
      }
      scrollEndTimeout.start(100, () => {
        programmaticScrollRef.current = true;
      });
    },
    onWheel: handleUserInteraction,
    onTouchMove: handleUserInteraction,
    onPointerMove: handleUserInteraction,
    onPointerEnter: handleUserInteraction,
    onKeyDown: handleUserInteraction
  });
  const viewportState = React9.useMemo(() => ({
    scrolling: scrollingX || scrollingY,
    hasOverflowX: !hiddenState.x,
    hasOverflowY: !hiddenState.y,
    overflowXStart: overflowEdges.xStart,
    overflowXEnd: overflowEdges.xEnd,
    overflowYStart: overflowEdges.yStart,
    overflowYEnd: overflowEdges.yEnd,
    cornerHidden: hiddenState.corner
  }), [scrollingX, scrollingY, hiddenState.x, hiddenState.y, hiddenState.corner, overflowEdges]);
  const element = useRenderElement("div", componentProps, {
    ref: [forwardedRef, viewportRef],
    state: viewportState,
    props: [props, elementProps],
    stateAttributesMapping: scrollAreaStateAttributesMapping
  });
  const contextValue = React9.useMemo(() => ({
    computeThumbPosition
  }), [computeThumbPosition]);
  return /* @__PURE__ */ jsx(ScrollAreaViewportContext.Provider, {
    value: contextValue,
    children: element
  });
});
if (process.env.NODE_ENV !== "production") ScrollAreaViewport.displayName = "ScrollAreaViewport";
function getHiddenState(viewport) {
  const y = viewport.clientHeight >= viewport.scrollHeight;
  const x = viewport.clientWidth >= viewport.scrollWidth;
  return {
    y,
    x,
    corner: y || x
  };
}
function mergeHiddenState(prevState, nextState) {
  if (prevState.y === nextState.y && prevState.x === nextState.x && prevState.corner === nextState.corner) {
    return prevState;
  }
  return nextState;
}
var ScrollAreaScrollbarContext = /* @__PURE__ */ React9.createContext(void 0);
if (process.env.NODE_ENV !== "production") ScrollAreaScrollbarContext.displayName = "ScrollAreaScrollbarContext";
function useScrollAreaScrollbarContext() {
  const context = React9.useContext(ScrollAreaScrollbarContext);
  if (context === void 0) {
    throw new Error(process.env.NODE_ENV !== "production" ? "Base UI: ScrollAreaScrollbarContext is missing. ScrollAreaScrollbar parts must be placed within <ScrollArea.Scrollbar>." : formatErrorMessage_default(54));
  }
  return context;
}

// ../../node_modules/@base-ui/react/esm/scroll-area/scrollbar/ScrollAreaScrollbarCssVars.js
var ScrollAreaScrollbarCssVars = /* @__PURE__ */ (function(ScrollAreaScrollbarCssVars2) {
  ScrollAreaScrollbarCssVars2["scrollAreaThumbHeight"] = "--scroll-area-thumb-height";
  ScrollAreaScrollbarCssVars2["scrollAreaThumbWidth"] = "--scroll-area-thumb-width";
  return ScrollAreaScrollbarCssVars2;
})({});
var ScrollAreaScrollbar = /* @__PURE__ */ React9.forwardRef(function ScrollAreaScrollbar2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className,
    orientation = "vertical",
    keepMounted = false
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className",
    "orientation",
    "keepMounted"
  ]);
  const {
    hovering,
    scrollingX,
    scrollingY,
    hiddenState,
    overflowEdges,
    scrollbarYRef,
    scrollbarXRef,
    viewportRef,
    thumbYRef,
    thumbXRef,
    handlePointerDown,
    handlePointerUp,
    rootId,
    thumbSize,
    hasMeasuredScrollbar
  } = useScrollAreaRootContext();
  const state = {
    hovering,
    scrolling: {
      horizontal: scrollingX,
      vertical: scrollingY
    }[orientation],
    orientation,
    hasOverflowX: !hiddenState.x,
    hasOverflowY: !hiddenState.y,
    overflowXStart: overflowEdges.xStart,
    overflowXEnd: overflowEdges.xEnd,
    overflowYStart: overflowEdges.yStart,
    overflowYEnd: overflowEdges.yEnd,
    cornerHidden: hiddenState.corner
  };
  const direction = useDirection();
  const hideTrackUntilMeasured = !hasMeasuredScrollbar && !keepMounted;
  React9.useEffect(() => {
    const viewportEl = viewportRef.current;
    const scrollbarEl = orientation === "vertical" ? scrollbarYRef.current : scrollbarXRef.current;
    if (!scrollbarEl) {
      return void 0;
    }
    function handleWheel(event) {
      if (!viewportEl || !scrollbarEl || event.ctrlKey) {
        return;
      }
      event.preventDefault();
      if (orientation === "vertical") {
        if (viewportEl.scrollTop === 0 && event.deltaY < 0) {
          return;
        }
      } else if (viewportEl.scrollLeft === 0 && event.deltaX < 0) {
        return;
      }
      if (orientation === "vertical") {
        if (viewportEl.scrollTop === viewportEl.scrollHeight - viewportEl.clientHeight && event.deltaY > 0) {
          return;
        }
      } else if (viewportEl.scrollLeft === viewportEl.scrollWidth - viewportEl.clientWidth && event.deltaX > 0) {
        return;
      }
      if (orientation === "vertical") {
        viewportEl.scrollTop += event.deltaY;
      } else {
        viewportEl.scrollLeft += event.deltaX;
      }
    }
    scrollbarEl.addEventListener("wheel", handleWheel, {
      passive: false
    });
    return () => {
      scrollbarEl.removeEventListener("wheel", handleWheel);
    };
  }, [orientation, scrollbarXRef, scrollbarYRef, viewportRef]);
  const props = __spreadProps(__spreadValues({}, rootId && {
    "data-id": `${rootId}-scrollbar`
  }), {
    onPointerDown(event) {
      if (event.button !== 0) {
        return;
      }
      if (event.currentTarget !== event.target) {
        return;
      }
      if (!viewportRef.current) {
        return;
      }
      if (thumbYRef.current && scrollbarYRef.current && orientation === "vertical") {
        const thumbYOffset = getOffset(thumbYRef.current, "margin", "y");
        const scrollbarYOffset = getOffset(scrollbarYRef.current, "padding", "y");
        const thumbHeight = thumbYRef.current.offsetHeight;
        const trackRectY = scrollbarYRef.current.getBoundingClientRect();
        const clickY = event.clientY - trackRectY.top - thumbHeight / 2 - scrollbarYOffset + thumbYOffset / 2;
        const scrollableContentHeight = viewportRef.current.scrollHeight;
        const viewportHeight = viewportRef.current.clientHeight;
        const maxThumbOffsetY = scrollbarYRef.current.offsetHeight - thumbHeight - scrollbarYOffset - thumbYOffset;
        const scrollRatioY = clickY / maxThumbOffsetY;
        const newScrollTop = scrollRatioY * (scrollableContentHeight - viewportHeight);
        viewportRef.current.scrollTop = newScrollTop;
      }
      if (thumbXRef.current && scrollbarXRef.current && orientation === "horizontal") {
        const thumbXOffset = getOffset(thumbXRef.current, "margin", "x");
        const scrollbarXOffset = getOffset(scrollbarXRef.current, "padding", "x");
        const thumbWidth = thumbXRef.current.offsetWidth;
        const trackRectX = scrollbarXRef.current.getBoundingClientRect();
        const clickX = event.clientX - trackRectX.left - thumbWidth / 2 - scrollbarXOffset + thumbXOffset / 2;
        const scrollableContentWidth = viewportRef.current.scrollWidth;
        const viewportWidth = viewportRef.current.clientWidth;
        const maxThumbOffsetX = scrollbarXRef.current.offsetWidth - thumbWidth - scrollbarXOffset - thumbXOffset;
        const scrollRatioX = clickX / maxThumbOffsetX;
        let newScrollLeft;
        if (direction === "rtl") {
          newScrollLeft = (1 - scrollRatioX) * (scrollableContentWidth - viewportWidth);
          if (viewportRef.current.scrollLeft <= 0) {
            newScrollLeft = -newScrollLeft;
          }
        } else {
          newScrollLeft = scrollRatioX * (scrollableContentWidth - viewportWidth);
        }
        viewportRef.current.scrollLeft = newScrollLeft;
      }
      handlePointerDown(event);
    },
    onPointerUp: handlePointerUp,
    style: __spreadValues(__spreadValues({
      position: "absolute",
      touchAction: "none",
      WebkitUserSelect: "none",
      userSelect: "none",
      visibility: hideTrackUntilMeasured ? "hidden" : void 0
    }, orientation === "vertical" && {
      top: 0,
      bottom: `var(${ScrollAreaRootCssVars.scrollAreaCornerHeight})`,
      insetInlineEnd: 0,
      [ScrollAreaScrollbarCssVars.scrollAreaThumbHeight]: `${thumbSize.height}px`
    }), orientation === "horizontal" && {
      insetInlineStart: 0,
      insetInlineEnd: `var(${ScrollAreaRootCssVars.scrollAreaCornerWidth})`,
      bottom: 0,
      [ScrollAreaScrollbarCssVars.scrollAreaThumbWidth]: `${thumbSize.width}px`
    })
  });
  const element = useRenderElement("div", componentProps, {
    ref: [forwardedRef, orientation === "vertical" ? scrollbarYRef : scrollbarXRef],
    state,
    props: [props, elementProps],
    stateAttributesMapping: scrollAreaStateAttributesMapping
  });
  const contextValue = React9.useMemo(() => ({
    orientation
  }), [orientation]);
  const isHidden = orientation === "vertical" ? hiddenState.y : hiddenState.x;
  const shouldRender = keepMounted || !isHidden;
  if (!shouldRender) {
    return null;
  }
  return /* @__PURE__ */ jsx(ScrollAreaScrollbarContext.Provider, {
    value: contextValue,
    children: element
  });
});
if (process.env.NODE_ENV !== "production") ScrollAreaScrollbar.displayName = "ScrollAreaScrollbar";
var ScrollAreaContent = /* @__PURE__ */ React9.forwardRef(function ScrollAreaContent2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className"
  ]);
  const contentWrapperRef = React9.useRef(null);
  const {
    computeThumbPosition
  } = useScrollAreaViewportContext();
  const {
    viewportState
  } = useScrollAreaRootContext();
  useIsoLayoutEffect(() => {
    if (typeof ResizeObserver === "undefined") {
      return void 0;
    }
    let hasInitialized = false;
    const ro = new ResizeObserver(() => {
      if (!hasInitialized) {
        hasInitialized = true;
        return;
      }
      computeThumbPosition();
    });
    if (contentWrapperRef.current) {
      ro.observe(contentWrapperRef.current);
    }
    return () => {
      ro.disconnect();
    };
  }, [computeThumbPosition]);
  const element = useRenderElement("div", componentProps, {
    ref: [forwardedRef, contentWrapperRef],
    state: viewportState,
    stateAttributesMapping: scrollAreaStateAttributesMapping,
    props: [{
      role: "presentation",
      style: {
        minWidth: "fit-content"
      }
    }, elementProps]
  });
  return element;
});
if (process.env.NODE_ENV !== "production") ScrollAreaContent.displayName = "ScrollAreaContent";
var ScrollAreaThumb = /* @__PURE__ */ React9.forwardRef(function ScrollAreaThumb2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className"
  ]);
  const {
    thumbYRef,
    thumbXRef,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp,
    setScrollingX,
    setScrollingY,
    hasMeasuredScrollbar
  } = useScrollAreaRootContext();
  const {
    orientation
  } = useScrollAreaScrollbarContext();
  const state = {
    orientation
  };
  const element = useRenderElement("div", componentProps, {
    ref: [forwardedRef, orientation === "vertical" ? thumbYRef : thumbXRef],
    state,
    props: [{
      onPointerDown: handlePointerDown,
      onPointerMove: handlePointerMove,
      onPointerUp(event) {
        if (orientation === "vertical") {
          setScrollingY(false);
        }
        if (orientation === "horizontal") {
          setScrollingX(false);
        }
        handlePointerUp(event);
      },
      style: __spreadValues(__spreadValues({
        visibility: hasMeasuredScrollbar ? void 0 : "hidden"
      }, orientation === "vertical" && {
        height: `var(${ScrollAreaScrollbarCssVars.scrollAreaThumbHeight})`
      }), orientation === "horizontal" && {
        width: `var(${ScrollAreaScrollbarCssVars.scrollAreaThumbWidth})`
      })
    }, elementProps]
  });
  return element;
});
if (process.env.NODE_ENV !== "production") ScrollAreaThumb.displayName = "ScrollAreaThumb";
var ScrollAreaCorner = /* @__PURE__ */ React9.forwardRef(function ScrollAreaCorner2(componentProps, forwardedRef) {
  const _a = componentProps, {
    render,
    className
  } = _a, elementProps = __objRest(_a, [
    "render",
    "className"
  ]);
  const {
    cornerRef,
    cornerSize,
    hiddenState
  } = useScrollAreaRootContext();
  const element = useRenderElement("div", componentProps, {
    ref: [forwardedRef, cornerRef],
    props: [{
      style: {
        position: "absolute",
        bottom: 0,
        insetInlineEnd: 0,
        width: cornerSize.width,
        height: cornerSize.height
      }
    }, elementProps]
  });
  if (hiddenState.corner) {
    return null;
  }
  return element;
});
if (process.env.NODE_ENV !== "production") ScrollAreaCorner.displayName = "ScrollAreaCorner";
function ContextMenu(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports4.Root, __spreadValues({ "data-slot": "context-menu" }, props));
}
function ContextMenuPortal(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports4.Portal, __spreadValues({ "data-slot": "context-menu-portal" }, props));
}
function ContextMenuTrigger3(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    index_parts_exports4.Trigger,
    __spreadValues({
      "data-slot": "context-menu-trigger",
      className: cn("select-none", className)
    }, props)
  );
}
function ContextMenuContent(_a) {
  var _b = _a, {
    className,
    align = "start",
    alignOffset = 4,
    side = "right",
    sideOffset = 0
  } = _b, props = __objRest(_b, [
    "className",
    "align",
    "alignOffset",
    "side",
    "sideOffset"
  ]);
  return /* @__PURE__ */ jsx(index_parts_exports4.Portal, { children: /* @__PURE__ */ jsx(
    index_parts_exports4.Positioner,
    {
      className: "isolate z-50 outline-none",
      align,
      alignOffset,
      side,
      sideOffset,
      children: /* @__PURE__ */ jsx(
        index_parts_exports4.Popup,
        __spreadValues({
          "data-slot": "context-menu-content",
          className: cn("z-50 max-h-(--available-height) min-w-36 origin-(--transform-origin) overflow-x-hidden overflow-y-auto rounded-md bg-popover p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 outline-none data-[side=bottom]:slide-in-from-top-2 data-[side=inline-end]:slide-in-from-left-2 data-[side=inline-start]:slide-in-from-right-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95", className)
        }, props)
      )
    }
  ) });
}
function ContextMenuGroup(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports4.Group, __spreadValues({ "data-slot": "context-menu-group" }, props));
}
function ContextMenuLabel(_a) {
  var _b = _a, {
    className,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "inset"
  ]);
  return /* @__PURE__ */ jsx(
    index_parts_exports4.GroupLabel,
    __spreadValues({
      "data-slot": "context-menu-label",
      "data-inset": inset,
      className: cn(
        "px-2 py-1 text-[12px] leading-[16px] text-muted-foreground data-inset:pl-7",
        className
      )
    }, props)
  );
}
function ContextMenuItem(_a) {
  var _b = _a, {
    className,
    inset,
    variant = "default"
  } = _b, props = __objRest(_b, [
    "className",
    "inset",
    "variant"
  ]);
  return /* @__PURE__ */ jsx(
    index_parts_exports4.Item,
    __spreadValues({
      "data-slot": "context-menu-item",
      "data-inset": inset,
      "data-variant": variant,
      className: cn(
        "group/context-menu-item relative flex cursor-default items-center gap-1.5 rounded-sm px-1.5 py-1 text-[13px] outline-hidden select-none focus:bg-hover data-inset:pl-7 data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive data-[variant=destructive]:focus:text-destructive-foreground dark:data-[variant=destructive]:focus:bg-destructive data-disabled:pointer-events-none data-disabled:text-disabled-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4 data-[variant=destructive]:*:[svg]:text-destructive",
        className
      )
    }, props)
  );
}
function ContextMenuSub(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(index_parts_exports4.SubmenuRoot, __spreadValues({ "data-slot": "context-menu-sub" }, props));
}
function ContextMenuSubTrigger(_a) {
  var _b = _a, {
    className,
    inset,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "inset",
    "children"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports4.SubmenuTrigger,
    __spreadProps(__spreadValues({
      "data-slot": "context-menu-sub-trigger",
      "data-inset": inset,
      className: cn(
        "flex cursor-default items-center gap-1.5 rounded-sm px-1.5 py-1 text-[13px] outline-hidden select-none focus:bg-hover data-inset:pl-7 data-open:bg-hover [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props), {
      children: [
        children,
        /* @__PURE__ */ jsx(ChevronRight, { className: "ml-auto" })
      ]
    })
  );
}
function ContextMenuSubContent(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(
    ContextMenuContent,
    __spreadValues({
      "data-slot": "context-menu-sub-content",
      className: "shadow-lg",
      side: "right"
    }, props)
  );
}
function ContextMenuCheckboxItem(_a) {
  var _b = _a, {
    className,
    children,
    checked,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "checked",
    "inset"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports4.CheckboxItem,
    __spreadProps(__spreadValues({
      "data-slot": "context-menu-checkbox-item",
      "data-inset": inset,
      className: cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-sm py-1 pl-1.5 pr-1.5 text-[13px] outline-hidden select-none focus:bg-hover data-disabled:pointer-events-none data-disabled:text-disabled-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      ),
      checked
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          Checkbox,
          {
            checked,
            className: "pointer-events-none",
            tabIndex: -1
          }
        ),
        children
      ]
    })
  );
}
function ContextMenuRadioGroup(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(
    index_parts_exports4.RadioGroup,
    __spreadValues({
      "data-slot": "context-menu-radio-group"
    }, props)
  );
}
function ContextMenuRadioItem(_a) {
  var _b = _a, {
    className,
    children,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "inset"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports4.RadioItem,
    __spreadProps(__spreadValues({
      "data-slot": "context-menu-radio-item",
      "data-inset": inset,
      className: cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-sm py-1 pl-7 pr-1.5 text-[13px] outline-hidden select-none focus:bg-hover data-disabled:pointer-events-none data-disabled:text-disabled-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "pointer-events-none absolute left-1.5 flex size-4 items-center justify-center",
            "data-slot": "context-menu-radio-item-indicator",
            children: /* @__PURE__ */ jsx(index_parts_exports4.RadioItemIndicator, { children: /* @__PURE__ */ jsx(Check, { className: "size-4" }) })
          }
        ),
        children
      ]
    })
  );
}
function ContextMenuSeparator(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    index_parts_exports4.Separator,
    __spreadValues({
      "data-slot": "context-menu-separator",
      className: cn("-mx-1 my-1 h-px bg-border", className)
    }, props)
  );
}
function ContextMenuShortcut(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "context-menu-shortcut",
      className: cn(
        "ml-auto text-[12px] tracking-widest text-muted-foreground group-focus/context-menu-item:text-muted-foreground",
        className
      )
    }, props)
  );
}
function ContextMenuItemIcon(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "context-menu-item-icon",
      className: cn(
        "pointer-events-none shrink-0 text-muted-foreground group-focus/context-menu-item:text-foreground [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function ContextMenuItemDescription(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "context-menu-item-description",
      className: cn(
        "block text-[12px] leading-[16px] text-muted-foreground font-normal group-focus/context-menu-item:text-foreground/70",
        className
      )
    }, props)
  );
}
function ContextMenuItemBadge(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "context-menu-item-badge",
      className: cn(
        "ml-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-muted px-1.5 text-[12px] font-semibold text-muted-foreground group-focus/context-menu-item:bg-accent-foreground/10 group-focus/context-menu-item:text-foreground",
        className
      )
    }, props)
  );
}
function ContextMenuSearch(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx("div", { className: "p-1", "data-slot": "context-menu-search", children: /* @__PURE__ */ jsx(Input, __spreadValues({ className: cn(className) }, props)) });
}
function ContextMenuEmpty(_a) {
  var _b = _a, {
    className,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "children"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "context-menu-empty",
      className: cn(
        "flex items-center justify-center px-2 py-4 text-[13px] text-muted-foreground",
        className
      )
    }, props), {
      children: children != null ? children : "No results found."
    })
  );
}
function ContextMenuLoading(_a) {
  var _b = _a, {
    className,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "children"
  ]);
  return /* @__PURE__ */ jsxs(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "context-menu-loading",
      className: cn(
        "flex items-center justify-center gap-2 px-2 py-4 text-[13px] text-muted-foreground",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          "svg",
          {
            className: "size-4 animate-spin",
            viewBox: "0 0 16 16",
            fill: "none",
            xmlns: "http://www.w3.org/2000/svg",
            children: /* @__PURE__ */ jsx(
              "path",
              {
                d: "M8 1.5A6.5 6.5 0 1 0 14.5 8",
                stroke: "currentColor",
                strokeWidth: "1.5",
                strokeLinecap: "round"
              }
            )
          }
        ),
        children != null ? children : "Loading..."
      ]
    })
  );
}
function ContextMenuFooter(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "context-menu-footer",
      className: cn(
        "flex items-center justify-end gap-2 border-t border-border p-2",
        className
      )
    }, props)
  );
}
var Target = forwardRef(
  (_a, ref) => {
    var _b = _a, { className, size = 16 } = _b, props = __objRest(_b, ["className", "size"]);
    return /* @__PURE__ */ jsxs("svg", __spreadProps(__spreadValues({ ref, className }, props), { width: size, height: size, viewBox: "0 0 16 16", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: [
      /* @__PURE__ */ jsx(
        "path",
        {
          fill: "currentColor",
          d: "M2.5 11v2.5H5V15H1.75a.75.75 0 0 1-.75-.75V11zM15 14.25a.75.75 0 0 1-.75.75H11v-1.5h2.5V11H15zM14.25 1a.75.75 0 0 1 .75.75V5h-1.5V2.5H11V1zM5 2.5H2.5V5H1V1.75A.75.75 0 0 1 1.75 1H5z"
        }
      ),
      /* @__PURE__ */ jsx(
        "path",
        {
          fill: "currentColor",
          fillRule: "evenodd",
          d: "M8.75 5.098a3 3 0 0 1 2.152 2.152H12.5v1.5h-1.598a3 3 0 0 1-2.152 2.151V12.5h-1.5v-1.599a3 3 0 0 1-2.153-2.156H3.5v-1.5h1.6a3 3 0 0 1 2.15-2.147V3.5h1.5zM8 6.5a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3",
          clipRule: "evenodd"
        }
      )
    ] }));
  }
);
Target.displayName = "Target";
var TreeContext = React9.createContext({
  highlightedId: null,
  setHighlighted: () => {
  },
  selectedId: null,
  setSelected: () => {
  }
});
var TreeParentContext = React9.createContext(null);
var TreePreviewAnchorContext = React9.createContext(null);
function Tree(_a) {
  var _b = _a, {
    className,
    defaultSelectedId,
    onSelect: onSelectProp,
    onFocusNode,
    onNodeMenu
  } = _b, props = __objRest(_b, [
    "className",
    "defaultSelectedId",
    "onSelect",
    "onFocusNode",
    "onNodeMenu"
  ]);
  const [highlightedId, setHighlighted] = React9.useState(null);
  const [selectedId, setSelectedInternal] = React9.useState(defaultSelectedId != null ? defaultSelectedId : null);
  const setSelected = React9.useCallback((id) => {
    setSelectedInternal(id);
    if (id) onSelectProp == null ? void 0 : onSelectProp(id);
  }, [onSelectProp]);
  return /* @__PURE__ */ jsx(TreeContext.Provider, { value: { highlightedId, setHighlighted, selectedId, setSelected, onFocusNode, onNodeMenu }, children: /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "tree",
      role: "tree",
      className: cn("flex flex-col", className)
    }, props)
  ) });
}
function TreeSection(_a) {
  var _b = _a, {
    className,
    label,
    defaultExpanded = true,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "label",
    "defaultExpanded",
    "children"
  ]);
  const [expanded, setExpanded] = React9.useState(defaultExpanded);
  return /* @__PURE__ */ jsxs(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "tree-section",
      className: cn("flex flex-col", className)
    }, props), {
      children: [
        /* @__PURE__ */ jsxs(
          "button",
          {
            "data-slot": "tree-item",
            "data-section": true,
            onClick: () => setExpanded(!expanded),
            onKeyDown: (e) => {
              var _a2, _b2;
              const tree = e.currentTarget.closest('[data-slot="tree"]');
              if (!tree) return;
              const items = Array.from(tree.querySelectorAll('[data-slot="tree-item"]'));
              const index = items.indexOf(e.currentTarget);
              switch (e.key) {
                case "ArrowDown": {
                  e.preventDefault();
                  (_a2 = items[index + 1]) == null ? void 0 : _a2.focus();
                  break;
                }
                case "ArrowUp": {
                  e.preventDefault();
                  (_b2 = items[index - 1]) == null ? void 0 : _b2.focus();
                  break;
                }
                case "ArrowRight": {
                  e.preventDefault();
                  if (!expanded) setExpanded(true);
                  break;
                }
                case "ArrowLeft": {
                  e.preventDefault();
                  if (expanded) setExpanded(false);
                  break;
                }
              }
            },
            className: "flex h-7 items-center gap-1 px-1 text-[12px] leading-[16px] text-muted-foreground hover:text-foreground",
            "aria-expanded": expanded,
            children: [
              expanded ? /* @__PURE__ */ jsx(ChevronDown, { className: "size-3 shrink-0" }) : /* @__PURE__ */ jsx(ChevronRight, { className: "size-3 shrink-0" }),
              /* @__PURE__ */ jsx("span", { className: "truncate", children: label })
            ]
          }
        ),
        expanded && children
      ]
    })
  );
}
var treeNodeCounter = 0;
function TreeNode(_a) {
  var _b = _a, {
    className,
    nodeId: nodeIdProp,
    icon,
    iconExpanded,
    label,
    trailing,
    preview,
    previewDelay = 700,
    selected = false,
    selectable = true,
    defaultExpanded = false,
    expanded: controlledExpanded,
    expandable = false,
    depth = 0,
    showTrailLine = true,
    onToggle,
    onSelect,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "nodeId",
    "icon",
    "iconExpanded",
    "label",
    "trailing",
    "preview",
    "previewDelay",
    "selected",
    "selectable",
    "defaultExpanded",
    "expanded",
    "expandable",
    "depth",
    "showTrailLine",
    "onToggle",
    "onSelect",
    "children"
  ]);
  const [internalExpanded, setInternalExpanded] = React9.useState(defaultExpanded);
  const isExpanded = controlledExpanded != null ? controlledExpanded : internalExpanded;
  const childCount = React9.Children.toArray(children).filter(
    (child) => React9.isValidElement(child)
  ).length;
  const isExpandable = expandable || childCount > 0;
  const idRef = React9.useRef(nodeIdProp != null ? nodeIdProp : `tree-node-${++treeNodeCounter}`);
  const { highlightedId, setHighlighted, selectedId, setSelected: setTreeSelected, onFocusNode, onNodeMenu } = React9.useContext(TreeContext);
  const parentId = React9.useContext(TreeParentContext);
  const isHighlighted = highlightedId === idRef.current;
  const isSelected = selected !== void 0 ? selected : selectedId === idRef.current;
  const handleClick = () => {
    if (isExpandable) {
      const next = !isExpanded;
      setInternalExpanded(next);
      setHighlighted(next ? idRef.current : parentId);
      onToggle == null ? void 0 : onToggle(next);
    }
    if (selectable) {
      setTreeSelected(idRef.current);
      onSelect == null ? void 0 : onSelect();
    }
  };
  const handleKeyDown = (e) => {
    const target = e.currentTarget;
    const tree = target.closest('[data-slot="tree"]');
    if (!tree) return;
    const items = Array.from(tree.querySelectorAll('[data-slot="tree-item"]'));
    const index = items.indexOf(target);
    switch (e.key) {
      case "ArrowDown": {
        e.preventDefault();
        const next = items[index + 1];
        if (next) next.focus();
        break;
      }
      case "ArrowUp": {
        e.preventDefault();
        const prev = items[index - 1];
        if (prev) prev.focus();
        break;
      }
      case "ArrowRight": {
        e.preventDefault();
        if (isExpandable && !isExpanded) {
          setInternalExpanded(true);
          setHighlighted(idRef.current);
          onToggle == null ? void 0 : onToggle(true);
        }
        break;
      }
      case "ArrowLeft": {
        e.preventDefault();
        if (isExpandable && isExpanded) {
          setInternalExpanded(false);
          setHighlighted(parentId);
          onToggle == null ? void 0 : onToggle(false);
        }
        break;
      }
    }
  };
  const activeIcon = isExpanded && iconExpanded ? iconExpanded : icon;
  const [previewOpen, setPreviewOpen] = React9.useState(false);
  const openTimerRef = React9.useRef(null);
  const closeTimerRef = React9.useRef(null);
  const cancelOpen = React9.useCallback(() => {
    if (openTimerRef.current) {
      clearTimeout(openTimerRef.current);
      openTimerRef.current = null;
    }
  }, []);
  const cancelClose = React9.useCallback(() => {
    if (closeTimerRef.current) {
      clearTimeout(closeTimerRef.current);
      closeTimerRef.current = null;
    }
  }, []);
  const scheduleOpen = React9.useCallback(() => {
    cancelClose();
    if (previewOpen) return;
    openTimerRef.current = setTimeout(() => setPreviewOpen(true), previewDelay);
  }, [cancelClose, previewOpen, previewDelay]);
  const scheduleClose = React9.useCallback(() => {
    cancelOpen();
    closeTimerRef.current = setTimeout(() => setPreviewOpen(false), 150);
  }, [cancelOpen]);
  React9.useEffect(() => () => {
    cancelOpen();
    cancelClose();
  }, [cancelOpen, cancelClose]);
  const rowButton = /* @__PURE__ */ jsxs(
    "button",
    __spreadProps(__spreadValues({
      "data-slot": "tree-item",
      "data-selected": selected || void 0,
      "data-expanded": isExpanded || void 0,
      role: "treeitem",
      "aria-selected": isSelected,
      "aria-expanded": isExpandable ? isExpanded : void 0,
      className: cn(
        "group/tree-item flex h-7 w-full items-center gap-1 rounded-sm px-1 text-[13px] leading-[20px] text-left transition-colors",
        selectable ? "hover:bg-hover" : "hover:bg-muted",
        isSelected && selectable && "bg-active",
        "text-foreground",
        className
      ),
      onClick: handleClick,
      onKeyDown: handleKeyDown,
      onMouseEnter: preview ? scheduleOpen : void 0,
      onMouseLeave: preview ? scheduleClose : void 0
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "flex shrink-0 items-center justify-end",
            style: { width: `${16 + depth * 8}px` },
            children: isExpandable ? /* @__PURE__ */ jsx("span", { className: cn(
              "flex size-4 items-center justify-center",
              isExpanded ? "text-muted-foreground" : "text-input"
            ), children: isExpanded ? /* @__PURE__ */ jsx(ChevronDown, { className: "size-3" }) : /* @__PURE__ */ jsx(ChevronRight, { className: "size-3" }) }) : /* @__PURE__ */ jsx("span", { className: "w-4" })
          }
        ),
        activeIcon && /* @__PURE__ */ jsx("span", { className: cn(
          "flex shrink-0 items-center [&_svg]:size-4",
          isSelected || isExpanded ? "text-foreground" : "text-muted-foreground"
        ), children: activeIcon }),
        /* @__PURE__ */ jsx("span", { className: "flex-1 truncate", children: label }),
        trailing ? /* @__PURE__ */ jsx("span", { className: "flex shrink-0 items-center gap-1 text-muted-foreground opacity-0 group-hover/tree-item:opacity-100 transition-opacity", children: trailing }) : selectable && /* @__PURE__ */ jsxs("span", { className: "flex shrink-0 items-center gap-0 opacity-0 group-hover/tree-item:opacity-100 transition-opacity", children: [
          isExpandable && /* @__PURE__ */ jsx(
            "button",
            {
              className: "flex size-6 items-center justify-center rounded-sm text-muted-foreground hover:bg-hover hover:text-foreground active:bg-press [&_svg]:size-4",
              "aria-label": "Focus here",
              title: "Focus here",
              onClick: (e) => {
                e.stopPropagation();
                onFocusNode == null ? void 0 : onFocusNode(idRef.current, label, activeIcon);
              },
              children: /* @__PURE__ */ jsx(Target, {})
            }
          ),
          /* @__PURE__ */ jsx(
            "button",
            {
              className: "flex size-6 items-center justify-center rounded-sm text-muted-foreground hover:bg-hover hover:text-foreground active:bg-press [&_svg]:size-4",
              "aria-label": "More options",
              title: "More options",
              onClick: (e) => {
                e.stopPropagation();
                onNodeMenu == null ? void 0 : onNodeMenu(idRef.current, label);
              },
              children: /* @__PURE__ */ jsx(Overflow, {})
            }
          )
        ] })
      ]
    })
  );
  const rowWithPreview = preview ? /* @__PURE__ */ jsxs(
    index_parts_exports$1.Root,
    {
      open: previewOpen,
      onOpenChange: (o) => {
        setPreviewOpen(o);
        if (!o) cancelOpen();
      },
      children: [
        /* @__PURE__ */ jsx(index_parts_exports$1.Trigger, { render: rowButton }),
        /* @__PURE__ */ jsx(index_parts_exports$1.Portal, { children: /* @__PURE__ */ jsx(
          index_parts_exports$1.Positioner,
          {
            side: "right",
            sideOffset: 4,
            align: "start",
            className: "z-50",
            children: /* @__PURE__ */ jsx(
              index_parts_exports$1.Popup,
              {
                "data-slot": "tree-item-preview",
                className: "outline-none",
                onMouseEnter: cancelClose,
                onMouseLeave: scheduleClose,
                children: preview
              }
            )
          }
        ) })
      ]
    }
  ) : rowButton;
  return /* @__PURE__ */ jsxs(Fragment, { children: [
    rowWithPreview,
    isExpanded && /* @__PURE__ */ jsx(TreeParentContext.Provider, { value: idRef.current, children: /* @__PURE__ */ jsxs(
      "div",
      {
        "data-slot": "tree-item-children",
        className: "relative overflow-visible",
        children: [
          showTrailLine && /* @__PURE__ */ jsx(
            "div",
            {
              className: cn(
                "absolute border-l pointer-events-none z-10 transition-colors",
                isHighlighted ? "border-foreground" : "border-border"
              ),
              style: {
                left: `${12 + depth * 8}px`,
                top: -10,
                bottom: 14
              }
            }
          ),
          childCount > 0 ? children : /* @__PURE__ */ jsx(
            "div",
            {
              className: "flex h-7 items-center text-[13px] text-muted-foreground",
              style: { paddingLeft: `${44 + (depth + 1) * 8}px` },
              children: "No items"
            }
          )
        ]
      }
    ) })
  ] });
}
function TreeNodeTag(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "tree-node-tag",
      className: cn(
        "inline-flex items-center gap-1 rounded bg-muted px-1.5 text-[12px] leading-[16px] text-muted-foreground",
        className
      )
    }, props)
  );
}
function TreeNodeRenderer({
  node,
  depth
}) {
  var _a, _b;
  const hasChildren = node.children && node.children.length > 0;
  const isLeaf = (_a = node.leaf) != null ? _a : false;
  const isSelectable = (_b = node.selectable) != null ? _b : true;
  const isExpandable = !isLeaf;
  return /* @__PURE__ */ jsx(
    TreeNode,
    {
      nodeId: node.id,
      icon: node.icon,
      iconExpanded: node.iconExpanded,
      label: node.label,
      depth,
      selectable: isSelectable,
      expandable: isExpandable,
      defaultExpanded: node.defaultExpanded,
      trailing: node.trailing,
      preview: node.preview,
      children: hasChildren && node.children.map((child) => /* @__PURE__ */ jsx(TreeNodeRenderer, { node: child, depth: depth + 1 }, child.id))
    }
  );
}
function DataTreeView({
  sections,
  className,
  onSelect,
  onFocusNode,
  onNodeMenu
}) {
  return /* @__PURE__ */ jsx(Tree, { className, onSelect, onFocusNode, onNodeMenu, children: sections.map((section, i) => {
    var _a;
    return /* @__PURE__ */ jsx(TreeSection, { label: section.label, defaultExpanded: (_a = section.defaultExpanded) != null ? _a : true, children: section.nodes.map((node) => /* @__PURE__ */ jsx(TreeNodeRenderer, { node, depth: 1 }, node.id)) }, i);
  }) });
}
function FileTreeView({
  nodes,
  className,
  onSelect,
  onFocusNode,
  onNodeMenu
}) {
  return /* @__PURE__ */ jsx(Tree, { className, onSelect, onFocusNode, onNodeMenu, children: nodes.map((node) => /* @__PURE__ */ jsx(TreeNodeRenderer, { node, depth: 0 }, node.id)) });
}
function FieldSet(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "fieldset",
    __spreadValues({
      "data-slot": "field-set",
      className: cn(
        "flex flex-col gap-4 has-[>[data-slot=checkbox-group]]:gap-3 has-[>[data-slot=radio-group]]:gap-3",
        className
      )
    }, props)
  );
}
function FieldLegend(_a) {
  var _b = _a, {
    className,
    variant = "legend"
  } = _b, props = __objRest(_b, [
    "className",
    "variant"
  ]);
  return /* @__PURE__ */ jsx(
    "legend",
    __spreadValues({
      "data-slot": "field-legend",
      "data-variant": variant,
      className: cn(
        "mb-1.5 font-semibold data-[variant=label]:text-[13px] data-[variant=legend]:text-[13px]",
        className
      )
    }, props)
  );
}
function FieldGroup(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "field-group",
      className: cn(
        "group/field-group @container/field-group flex w-full flex-col gap-5 data-[slot=checkbox-group]:gap-3 *:data-[slot=field-group]:gap-4",
        className
      )
    }, props)
  );
}
var fieldVariants = cva(
  "group/field flex w-full gap-2 data-[invalid=true]:text-destructive",
  {
    variants: {
      orientation: {
        vertical: "flex-col *:w-full [&>.sr-only]:w-auto",
        horizontal: "flex-row items-center has-[>[data-slot=field-content]]:items-start *:data-[slot=field-label]:flex-auto has-[>[data-slot=field-content]]:[&>[role=checkbox],[role=radio]]:mt-px",
        responsive: "flex-col *:w-full @md/field-group:flex-row @md/field-group:items-center @md/field-group:*:w-auto @md/field-group:has-[>[data-slot=field-content]]:items-start @md/field-group:*:data-[slot=field-label]:flex-auto [&>.sr-only]:w-auto @md/field-group:has-[>[data-slot=field-content]]:[&>[role=checkbox],[role=radio]]:mt-px"
      }
    },
    defaultVariants: {
      orientation: "vertical"
    }
  }
);
function Field(_a) {
  var _b = _a, {
    className,
    orientation = "vertical"
  } = _b, props = __objRest(_b, [
    "className",
    "orientation"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      role: "group",
      "data-slot": "field",
      "data-orientation": orientation,
      className: cn(fieldVariants({ orientation }), className)
    }, props)
  );
}
function FieldContent(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "field-content",
      className: cn(
        "group/field-content flex flex-1 flex-col gap-0.5 leading-snug",
        className
      )
    }, props)
  );
}
function FieldLabel(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    Label,
    __spreadValues({
      "data-slot": "field-label",
      className: cn(
        "group/field-label peer/field-label flex w-fit gap-2 leading-snug group-data-[disabled=true]/field:opacity-50 has-data-checked:border-primary/30 has-data-checked:bg-primary/5 has-[>[data-slot=field]]:rounded-lg has-[>[data-slot=field]]:border *:data-[slot=field]:p-2.5 dark:has-data-checked:border-primary/20 dark:has-data-checked:bg-primary/10",
        "has-[>[data-slot=field]]:w-full has-[>[data-slot=field]]:flex-col",
        className
      )
    }, props)
  );
}
function FieldTitle(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "field-label",
      className: cn(
        "flex w-fit items-center gap-2 text-[13px] leading-snug font-semibold group-data-[disabled=true]/field:opacity-50",
        className
      )
    }, props)
  );
}
function FieldDescription(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "p",
    __spreadValues({
      "data-slot": "field-description",
      className: cn(
        "text-left text-[13px] leading-normal font-normal text-muted-foreground group-has-data-horizontal/field:text-balance [[data-variant=legend]+&]:-mt-1.5",
        "last:mt-0 nth-last-2:-mt-1",
        "[&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary",
        className
      )
    }, props)
  );
}
function FieldSeparator(_a) {
  var _b = _a, {
    children,
    className
  } = _b, props = __objRest(_b, [
    "children",
    "className"
  ]);
  return /* @__PURE__ */ jsxs(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "field-separator",
      "data-content": !!children,
      className: cn(
        "relative -my-2 h-5 text-[13px] group-data-[variant=outline]/field-group:-mb-2",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(Separator$1, { className: "absolute inset-0 top-1/2" }),
        children && /* @__PURE__ */ jsx(
          "span",
          {
            className: "relative mx-auto block w-fit bg-background px-2 text-muted-foreground",
            "data-slot": "field-separator-content",
            children
          }
        )
      ]
    })
  );
}
function FieldError(_a) {
  var _b = _a, {
    className,
    children,
    errors
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "errors"
  ]);
  const content = useMemo(() => {
    var _a2;
    if (children) {
      return children;
    }
    if (!(errors == null ? void 0 : errors.length)) {
      return null;
    }
    const uniqueErrors = [
      ...new Map(errors.map((error) => [error == null ? void 0 : error.message, error])).values()
    ];
    if ((uniqueErrors == null ? void 0 : uniqueErrors.length) == 1) {
      return (_a2 = uniqueErrors[0]) == null ? void 0 : _a2.message;
    }
    return /* @__PURE__ */ jsx("ul", { className: "ml-4 flex list-disc flex-col gap-1", children: uniqueErrors.map(
      (error, index) => (error == null ? void 0 : error.message) && /* @__PURE__ */ jsx("li", { children: error.message }, index)
    ) });
  }, [children, errors]);
  if (!content) {
    return null;
  }
  return /* @__PURE__ */ jsx(
    "div",
    __spreadProps(__spreadValues({
      role: "alert",
      "data-slot": "field-error",
      className: cn("text-[13px] font-normal text-destructive", className)
    }, props), {
      children: content
    })
  );
}
function ItemGroup(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      role: "list",
      "data-slot": "item-group",
      className: cn(
        "group/item-group flex w-full flex-col gap-4 has-data-[size=sm]:gap-2.5 has-data-[size=xs]:gap-2",
        className
      )
    }, props)
  );
}
function ItemSeparator(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    Separator$1,
    __spreadValues({
      "data-slot": "item-separator",
      orientation: "horizontal",
      className: cn("my-2", className)
    }, props)
  );
}
var itemVariants = cva(
  "group/item flex w-full flex-wrap items-center rounded-lg border text-[13px] transition-colors duration-100 outline-none focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 [a]:transition-colors [a]:hover:bg-muted",
  {
    variants: {
      variant: {
        default: "border-transparent",
        outline: "border-border",
        muted: "border-transparent bg-muted/50"
      },
      size: {
        default: "gap-2.5 px-3 py-2.5",
        sm: "gap-2.5 px-3 py-2.5",
        xs: "gap-2 px-2.5 py-2 in-data-[slot=dropdown-menu-content]:p-0"
      }
    },
    defaultVariants: {
      variant: "default",
      size: "default"
    }
  }
);
function Item(_a) {
  var _b = _a, {
    className,
    variant = "default",
    size = "default",
    render
  } = _b, props = __objRest(_b, [
    "className",
    "variant",
    "size",
    "render"
  ]);
  return useRender({
    defaultTagName: "div",
    props: mergeProps(
      {
        className: cn(itemVariants({ variant, size, className }))
      },
      props
    ),
    render,
    state: {
      slot: "item",
      variant,
      size
    }
  });
}
var itemMediaVariants = cva(
  "flex shrink-0 items-center justify-center gap-2 group-has-data-[slot=item-description]/item:translate-y-0.5 group-has-data-[slot=item-description]/item:self-start [&_svg]:pointer-events-none",
  {
    variants: {
      variant: {
        default: "bg-transparent",
        icon: "[&_svg:not([class*='size-'])]:size-4",
        image: "size-10 overflow-hidden rounded-sm group-data-[size=sm]/item:size-8 group-data-[size=xs]/item:size-6 [&_img]:size-full [&_img]:object-cover"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  }
);
function ItemMedia(_a) {
  var _b = _a, {
    className,
    variant = "default"
  } = _b, props = __objRest(_b, [
    "className",
    "variant"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-media",
      "data-variant": variant,
      className: cn(itemMediaVariants({ variant, className }))
    }, props)
  );
}
function ItemContent(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-content",
      className: cn(
        "flex flex-1 flex-col gap-1 group-data-[size=xs]/item:gap-0 [&+[data-slot=item-content]]:flex-none",
        className
      )
    }, props)
  );
}
function ItemTitle(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-title",
      className: cn(
        "line-clamp-1 flex w-fit items-center gap-2 text-[13px] leading-snug font-semibold underline-offset-4",
        className
      )
    }, props)
  );
}
function ItemDescription(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "p",
    __spreadValues({
      "data-slot": "item-description",
      className: cn(
        "line-clamp-2 text-left text-[13px] leading-normal font-normal text-muted-foreground group-data-[size=xs]/item:text-[12px] [&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary",
        className
      )
    }, props)
  );
}
function ItemActions(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-actions",
      className: cn("flex items-center gap-2", className)
    }, props)
  );
}
function ItemHeader(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-header",
      className: cn(
        "flex basis-full items-center justify-between gap-2",
        className
      )
    }, props)
  );
}
function ItemFooter(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "item-footer",
      className: cn(
        "flex basis-full items-center justify-between gap-2",
        className
      )
    }, props)
  );
}
function Kbd(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "kbd",
    __spreadValues({
      "data-slot": "kbd",
      className: cn(
        "pointer-events-none inline-flex h-5 w-fit min-w-5 items-center justify-center gap-1 rounded-sm bg-muted px-1 font-sans text-[12px] font-semibold text-muted-foreground select-none in-data-[slot=tooltip-content]:bg-background/20 in-data-[slot=tooltip-content]:text-background dark:in-data-[slot=tooltip-content]:bg-background/10 [&_svg:not([class*='size-'])]:size-3",
        className
      )
    }, props)
  );
}
function KbdGroup(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    "kbd",
    __spreadValues({
      "data-slot": "kbd-group",
      className: cn("inline-flex items-center gap-1", className)
    }, props)
  );
}
function Menubar3(_a) {
  var _b = _a, { className } = _b, props = __objRest(_b, ["className"]);
  return /* @__PURE__ */ jsx(
    Menubar,
    __spreadValues({
      "data-slot": "menubar",
      className: cn(
        "flex h-8 items-center gap-0.5 rounded-lg border p-[3px]",
        className
      )
    }, props)
  );
}
function MenubarMenu(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(DropdownMenu, __spreadValues({ "data-slot": "menubar-menu" }, props));
}
function MenubarGroup(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(DropdownMenuGroup, __spreadValues({ "data-slot": "menubar-group" }, props));
}
function MenubarPortal(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(DropdownMenuPortal, __spreadValues({ "data-slot": "menubar-portal" }, props));
}
function MenubarTrigger(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuTrigger,
    __spreadValues({
      "data-slot": "menubar-trigger",
      className: cn(
        "flex items-center rounded-sm px-1.5 py-[2px] text-[13px] font-semibold outline-hidden select-none hover:bg-muted aria-expanded:bg-muted",
        className
      )
    }, props)
  );
}
function MenubarContent2(_a) {
  var _b = _a, {
    className,
    align = "start",
    alignOffset = -4,
    sideOffset = 8
  } = _b, props = __objRest(_b, [
    "className",
    "align",
    "alignOffset",
    "sideOffset"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuContent,
    __spreadValues({
      "data-slot": "menubar-content",
      align,
      alignOffset,
      sideOffset,
      className: cn("min-w-36 rounded-md bg-popover p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 duration-100 data-[side=bottom]:slide-in-from-top-2 data-[side=inline-end]:slide-in-from-left-2 data-[side=inline-start]:slide-in-from-right-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95", className)
    }, props)
  );
}
function MenubarItem(_a) {
  var _b = _a, {
    className,
    inset,
    variant = "default"
  } = _b, props = __objRest(_b, [
    "className",
    "inset",
    "variant"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuItem,
    __spreadValues({
      "data-slot": "menubar-item",
      "data-inset": inset,
      "data-variant": variant,
      className: cn(
        "group/menubar-item gap-1.5 rounded-sm px-1.5 py-1 text-[13px] focus:bg-hover data-inset:pl-7 data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive data-[variant=destructive]:focus:text-destructive-foreground dark:data-[variant=destructive]:focus:bg-destructive data-disabled:text-disabled-foreground [&_svg:not([class*='size-'])]:size-4 data-[variant=destructive]:*:[svg]:text-destructive!",
        className
      )
    }, props)
  );
}
function MenubarCheckboxItem(_a) {
  var _b = _a, {
    className,
    children,
    checked,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "checked",
    "inset"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports.CheckboxItem,
    __spreadProps(__spreadValues({
      "data-slot": "menubar-checkbox-item",
      "data-inset": inset,
      className: cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-sm py-1 pl-1.5 pr-1.5 text-[13px] outline-hidden select-none focus:bg-hover data-disabled:pointer-events-none data-disabled:text-disabled-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      ),
      checked
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          Checkbox,
          {
            checked,
            className: "pointer-events-none",
            tabIndex: -1
          }
        ),
        children
      ]
    })
  );
}
function MenubarRadioGroup(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(DropdownMenuRadioGroup, __spreadValues({ "data-slot": "menubar-radio-group" }, props));
}
function MenubarRadioItem(_a) {
  var _b = _a, {
    className,
    children,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "inset"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports.RadioItem,
    __spreadProps(__spreadValues({
      "data-slot": "menubar-radio-item",
      "data-inset": inset,
      className: cn(
        "relative flex min-h-7 cursor-default items-center gap-2 rounded-sm py-1 pl-7 pr-1.5 text-[13px] outline-hidden select-none focus:bg-hover data-disabled:pointer-events-none data-disabled:text-disabled-foreground [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          "span",
          {
            className: "pointer-events-none absolute left-1.5 flex size-4 items-center justify-center",
            "data-slot": "menubar-radio-item-indicator",
            children: /* @__PURE__ */ jsx(index_parts_exports.RadioItemIndicator, { children: /* @__PURE__ */ jsx(Check, { className: "size-4" }) })
          }
        ),
        children
      ]
    })
  );
}
function MenubarLabel(_a) {
  var _b = _a, {
    className,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "inset"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuLabel,
    __spreadValues({
      "data-slot": "menubar-label",
      "data-inset": inset,
      className: cn(
        "px-1.5 py-1 text-[12px] leading-[16px] text-muted-foreground data-inset:pl-7",
        className
      )
    }, props)
  );
}
function MenubarSeparator(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuSeparator,
    __spreadValues({
      "data-slot": "menubar-separator",
      className: cn("-mx-1 my-1 h-px bg-border", className)
    }, props)
  );
}
function MenubarShortcut(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuShortcut,
    __spreadValues({
      "data-slot": "menubar-shortcut",
      className: cn(
        "ml-auto text-[12px] tracking-widest text-muted-foreground group-focus/menubar-item:text-muted-foreground",
        className
      )
    }, props)
  );
}
function MenubarSub(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(DropdownMenuSub, __spreadValues({ "data-slot": "menubar-sub" }, props));
}
function MenubarSubTrigger(_a) {
  var _b = _a, {
    className,
    inset
  } = _b, props = __objRest(_b, [
    "className",
    "inset"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuSubTrigger,
    __spreadValues({
      "data-slot": "menubar-sub-trigger",
      "data-inset": inset,
      className: cn(
        "gap-1.5 rounded-sm px-1.5 py-1 text-[13px] focus:bg-hover data-inset:pl-7 data-open:bg-hover [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function MenubarSubContent(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    DropdownMenuSubContent,
    __spreadValues({
      "data-slot": "menubar-sub-content",
      className: cn("min-w-32 rounded-md bg-popover p-1 text-popover-foreground shadow-lg ring-1 ring-foreground/10 duration-100 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95", className)
    }, props)
  );
}
function MenubarItemIcon(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "menubar-item-icon",
      className: cn(
        "pointer-events-none shrink-0 text-muted-foreground group-focus/menubar-item:text-foreground [&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function MenubarItemDescription(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "menubar-item-description",
      className: cn(
        "block text-[12px] leading-[16px] text-muted-foreground font-normal group-focus/menubar-item:text-foreground/70",
        className
      )
    }, props)
  );
}
function MenubarItemBadge(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "menubar-item-badge",
      className: cn(
        "ml-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-muted px-1.5 text-[12px] font-semibold text-muted-foreground group-focus/menubar-item:bg-accent-foreground/10 group-focus/menubar-item:text-foreground",
        className
      )
    }, props)
  );
}
function MenubarSearch(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx("div", { className: "p-1", "data-slot": "menubar-search", children: /* @__PURE__ */ jsx(
    Input,
    __spreadValues({
      type: "text",
      className: cn("w-full", className)
    }, props)
  ) });
}
function MenubarEmpty(_a) {
  var _b = _a, {
    className,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "children"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "menubar-empty",
      className: cn(
        "flex items-center justify-center px-2 py-4 text-[13px] text-muted-foreground",
        className
      )
    }, props), {
      children: children != null ? children : "No results found."
    })
  );
}
function MenubarLoading(_a) {
  var _b = _a, {
    className,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "children"
  ]);
  return /* @__PURE__ */ jsxs(
    "div",
    __spreadProps(__spreadValues({
      "data-slot": "menubar-loading",
      className: cn(
        "flex items-center justify-center gap-2 px-2 py-4 text-[13px] text-muted-foreground",
        className
      )
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          "svg",
          {
            className: "size-4 animate-spin",
            viewBox: "0 0 16 16",
            fill: "none",
            xmlns: "http://www.w3.org/2000/svg",
            children: /* @__PURE__ */ jsx(
              "path",
              {
                d: "M8 1.5A6.5 6.5 0 1 0 14.5 8",
                stroke: "currentColor",
                strokeWidth: "1.5",
                strokeLinecap: "round"
              }
            )
          }
        ),
        children != null ? children : "Loading..."
      ]
    })
  );
}
function MenubarFooter(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "menubar-footer",
      className: cn(
        "flex items-center justify-end gap-2 border-t border-border p-2",
        className
      )
    }, props)
  );
}
function NativeSelect(_a) {
  var _b = _a, {
    className,
    size = "default"
  } = _b, props = __objRest(_b, [
    "className",
    "size"
  ]);
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        "group/native-select relative w-fit has-[select:disabled]:opacity-50",
        className
      ),
      "data-slot": "native-select-wrapper",
      "data-size": size,
      children: [
        /* @__PURE__ */ jsx(
          "select",
          __spreadValues({
            "data-slot": "native-select",
            "data-size": size,
            className: "h-8 w-full min-w-0 appearance-none rounded-lg border border-input bg-transparent py-1 pr-8 pl-2.5 text-[13px] transition-colors outline-none select-none selection:bg-primary selection:text-primary-foreground placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:pointer-events-none disabled:cursor-not-allowed aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 data-[size=sm]:h-7 data-[size=sm]:rounded-[min(var(--radius-md),10px)] data-[size=sm]:py-0.5 dark:bg-input/30 dark:hover:bg-input/50 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40"
          }, props)
        ),
        /* @__PURE__ */ jsx(ChevronDown, { className: "pointer-events-none absolute top-1/2 right-2.5 size-4 -translate-y-1/2 text-muted-foreground select-none", "aria-hidden": "true", "data-slot": "native-select-icon" })
      ]
    }
  );
}
function NativeSelectOption(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx("option", __spreadValues({ "data-slot": "native-select-option" }, props));
}
function NativeSelectOptGroup(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "optgroup",
    __spreadValues({
      "data-slot": "native-select-optgroup",
      className: cn(className)
    }, props)
  );
}
function PageHeader(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "page-header",
      className: cn(
        "flex flex-col gap-2 px-4 py-2",
        className
      )
    }, props)
  );
}
function PageHeaderTitleBar(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "page-header-title-bar",
      className: cn(
        "flex items-center justify-between",
        className
      )
    }, props)
  );
}
function PageHeaderBack(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  var _a2;
  return /* @__PURE__ */ jsx(
    "button",
    __spreadProps(__spreadValues({
      "data-slot": "page-header-back",
      "aria-label": "Go back",
      className: cn(
        "inline-flex size-8 shrink-0 items-center justify-center rounded-sm text-foreground hover:bg-hover",
        "[&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props), {
      children: (_a2 = props.children) != null ? _a2 : /* @__PURE__ */ jsx(
        "svg",
        {
          width: "16",
          height: "16",
          viewBox: "0 0 16 16",
          fill: "none",
          xmlns: "http://www.w3.org/2000/svg",
          className: "size-4",
          children: /* @__PURE__ */ jsx(
            "path",
            {
              d: "M10 4L6 8L10 12",
              stroke: "currentColor",
              strokeWidth: "1.5",
              strokeLinecap: "round",
              strokeLinejoin: "round"
            }
          )
        }
      )
    })
  );
}
function PageHeaderTitle(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "page-header-title",
      className: cn(
        "flex items-center gap-2",
        "[&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function PageHeaderActions(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "page-header-actions",
      className: cn(
        "flex shrink-0 items-center gap-2",
        "[&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function PlatformHeader(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "header",
    __spreadValues({
      "data-slot": "platform-header",
      className: cn(
        "flex h-12 items-center justify-between px-3 py-2",
        className
      )
    }, props)
  );
}
function PlatformHeaderLeft(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "platform-header-left",
      className: cn(
        "flex items-center gap-1",
        "[&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function PlatformHeaderCenter(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "platform-header-center",
      className: cn(
        "flex w-[552px] items-center",
        className
      )
    }, props)
  );
}
function PlatformHeaderRight(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "div",
    __spreadValues({
      "data-slot": "platform-header-right",
      className: cn(
        "flex items-center gap-1",
        "[&_svg:not([class*='size-'])]:size-4",
        className
      )
    }, props)
  );
}
function PlatformHeaderBadge(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    "span",
    __spreadValues({
      "data-slot": "platform-header-badge",
      className: cn(
        "text-[14px] leading-[20px] text-foreground",
        className
      )
    }, props)
  );
}
function ResizablePanelGroup(_a) {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    ResizablePrimitive.Group,
    __spreadValues({
      "data-slot": "resizable-panel-group",
      className: cn(
        "flex h-full w-full aria-[orientation=vertical]:flex-col",
        className
      )
    }, props)
  );
}
function ResizablePanel(_a) {
  var props = __objRest(_a, []);
  return /* @__PURE__ */ jsx(ResizablePrimitive.Panel, __spreadValues({ "data-slot": "resizable-panel" }, props));
}
function ResizableHandle(_a) {
  var _b = _a, {
    withHandle,
    className
  } = _b, props = __objRest(_b, [
    "withHandle",
    "className"
  ]);
  return /* @__PURE__ */ jsx(
    ResizablePrimitive.Separator,
    __spreadProps(__spreadValues({
      "data-slot": "resizable-handle",
      className: cn(
        "relative flex w-px items-center justify-center bg-border ring-offset-background after:absolute after:inset-y-0 after:left-1/2 after:w-1 after:-translate-x-1/2 focus-visible:ring-1 focus-visible:ring-ring focus-visible:outline-hidden aria-[orientation=horizontal]:h-px aria-[orientation=horizontal]:w-full aria-[orientation=horizontal]:after:left-0 aria-[orientation=horizontal]:after:h-1 aria-[orientation=horizontal]:after:w-full aria-[orientation=horizontal]:after:translate-x-0 aria-[orientation=horizontal]:after:-translate-y-1/2 [&[aria-orientation=horizontal]>div]:rotate-90",
        className
      )
    }, props), {
      children: withHandle && /* @__PURE__ */ jsx("div", { className: "z-10 flex h-6 w-1 shrink-0 rounded-lg bg-border" })
    })
  );
}
function ScrollArea(_a) {
  var _b = _a, {
    className,
    children
  } = _b, props = __objRest(_b, [
    "className",
    "children"
  ]);
  return /* @__PURE__ */ jsxs(
    index_parts_exports5.Root,
    __spreadProps(__spreadValues({
      "data-slot": "scroll-area",
      className: cn("relative", className)
    }, props), {
      children: [
        /* @__PURE__ */ jsx(
          index_parts_exports5.Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsx(ScrollBar, {}),
        /* @__PURE__ */ jsx(index_parts_exports5.Corner, {})
      ]
    })
  );
}
function ScrollBar(_a) {
  var _b = _a, {
    className,
    orientation = "vertical"
  } = _b, props = __objRest(_b, [
    "className",
    "orientation"
  ]);
  return /* @__PURE__ */ jsx(
    index_parts_exports5.Scrollbar,
    __spreadProps(__spreadValues({
      "data-slot": "scroll-area-scrollbar",
      "data-orientation": orientation,
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none data-horizontal:h-2.5 data-horizontal:flex-col data-horizontal:border-t data-horizontal:border-t-transparent data-vertical:h-full data-vertical:w-2.5 data-vertical:border-l data-vertical:border-l-transparent",
        className
      )
    }, props), {
      children: /* @__PURE__ */ jsx(
        index_parts_exports5.Thumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "relative flex-1 rounded-full bg-border"
        }
      )
    })
  );
}
var ToggleGroupContext = React9.createContext({
  size: "md",
  variant: "default",
  spacing: 0,
  orientation: "horizontal"
});
function ToggleGroup2(_a) {
  var _b = _a, {
    className,
    variant,
    size,
    spacing = 0,
    orientation = "horizontal",
    children
  } = _b, props = __objRest(_b, [
    "className",
    "variant",
    "size",
    "spacing",
    "orientation",
    "children"
  ]);
  return /* @__PURE__ */ jsx(
    ToggleGroup,
    __spreadProps(__spreadValues({
      "data-slot": "toggle-group",
      "data-variant": variant,
      "data-size": size,
      "data-spacing": spacing,
      "data-orientation": orientation,
      className: cn(
        "group/toggle-group inline-flex items-center rounded-sm bg-accent p-0.5",
        variant === "outline" && "border border-input",
        orientation === "vertical" && "flex-col items-stretch",
        className
      )
    }, props), {
      children: /* @__PURE__ */ jsx(
        ToggleGroupContext.Provider,
        {
          value: { variant, size, spacing, orientation },
          children
        }
      )
    })
  );
}
function ToggleGroupItem(_a) {
  var _b = _a, {
    className,
    children,
    variant = "default",
    size = "md"
  } = _b, props = __objRest(_b, [
    "className",
    "children",
    "variant",
    "size"
  ]);
  const context = React9.useContext(ToggleGroupContext);
  const resolvedSize = context.size || size;
  return /* @__PURE__ */ jsx(
    Toggle,
    __spreadProps(__spreadValues({
      "data-slot": "toggle-group-item",
      className: cn(
        "inline-flex items-center justify-center gap-1",
        "rounded-sm border border-transparent",
        "text-[13px] leading-[20px] font-normal whitespace-nowrap",
        "transition-all outline-none select-none",
        "text-foreground",
        "hover:bg-hover",
        "active:bg-press",
        "focus-visible:border-2 focus-visible:border-ring focus:z-10",
        "disabled:pointer-events-none disabled:text-disabled-foreground",
        "aria-pressed:bg-background aria-pressed:border-primary aria-pressed:text-accent-foreground aria-pressed:shadow-xs",
        "data-[state=on]:bg-background data-[state=on]:border-primary data-[state=on]:text-accent-foreground data-[state=on]:shadow-xs",
        "[&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
        resolvedSize === "sm" ? "h-6 min-w-6 px-2" : "h-8 min-w-8 px-3",
        className
      )
    }, props), {
      children
    })
  );
}

// src/rules/composition-rules.ts
var compositionRules = [
  // ─── Button Rules ───
  {
    id: "button-link-no-icon",
    component: "Button",
    rule: "Link variant must not have a leading icon (ButtonIcon).",
    rationale: "Link buttons are inline text affordances. Icons make them look like regular buttons, breaking the visual hierarchy.",
    severity: "error"
  },
  {
    id: "button-link-no-chevron",
    component: "Button",
    rule: "Link variant must not have a trailing chevron (ButtonChevron).",
    rationale: "Links don't trigger menus. The only acceptable trailing element on a link is an external-link icon when opening a new window.",
    severity: "error"
  },
  {
    id: "button-ghost-prefer-no-icon",
    component: "Button",
    rule: "Ghost buttons should prefer icon-only (size icon-sm/icon-md) over icon+label.",
    rationale: "Ghost buttons with icons and labels compete visually with outlined buttons. Use ghost for icon-only toolbar actions.",
    severity: "warning"
  },
  {
    id: "button-icon-only-needs-aria",
    component: "Button",
    rule: "Icon-only buttons (size icon-sm/icon-md) must have aria-label.",
    rationale: "Icons alone are not accessible. Screen readers need a text label.",
    severity: "error"
  },
  {
    id: "button-destructive-confirm",
    component: "Button",
    rule: "Destructive buttons should only appear inside AlertDialog or as the final action in a confirmed flow.",
    rationale: "Red filled buttons signal irreversible danger. They should never be the first thing a user sees without context.",
    severity: "warning"
  },
  // ─── Menu Button Rules ───
  {
    id: "menu-button-no-icon",
    component: "Button + DropdownMenu",
    rule: "Menu trigger buttons (with ButtonChevron) should not have a leading icon.",
    rationale: "Menu buttons already have a trailing chevron as their affordance. A leading icon adds visual noise and makes the button look like an action button rather than a selector.",
    severity: "warning"
  },
  {
    id: "menu-button-outline-or-secondary",
    component: "Button + DropdownMenu",
    rule: "Menu trigger buttons should use outline or secondary variant, not primary.",
    rationale: "Primary is reserved for the main action on a page. Menu buttons are selectors, not primary actions.",
    severity: "warning"
  },
  // ─── Link Rules ───
  {
    id: "link-external-icon-only",
    component: "Link / Button[variant=link]",
    rule: "The only icon allowed on a link is a trailing external-link icon (NewWindow) when the link opens in a new tab.",
    rationale: "Links are text-only affordances. The external-link icon is the only visual cue that's informational rather than decorative.",
    severity: "error"
  },
  // ─── DropdownMenu Rules ───
  {
    id: "menu-item-icon-consistency",
    component: "DropdownMenuItem",
    rule: "If one menu item in a group has an icon, all items in that group must have icons.",
    rationale: "Mixed icon/no-icon items create misaligned labels. Either all items have icons or none do.",
    severity: "error"
  },
  {
    id: "menu-destructive-last",
    component: "DropdownMenuItem",
    rule: "Destructive menu items must appear at the bottom, separated by a DropdownMenuSeparator.",
    rationale: "Dangerous actions should be visually separated from safe actions to prevent accidental clicks.",
    severity: "error"
  },
  {
    id: "menu-shortcut-consistency",
    component: "DropdownMenuShortcut",
    rule: "Keyboard shortcuts should use the \u2318/\u21E7/\u2325/\u2303 symbols, not Cmd/Shift/Alt/Ctrl text.",
    rationale: "Consistent with macOS conventions and the Databricks platform.",
    severity: "warning"
  },
  // ─── Alert Rules ───
  {
    id: "alert-always-has-icon",
    component: "Alert",
    rule: "Alerts must always include an AlertIcon with the variant-appropriate icon (DangerFill, WarningFill, InfoFill, CheckCircleFill).",
    rationale: "The icon provides immediate visual distinction between alert severities. Without it, users rely solely on color which is not accessible.",
    severity: "error"
  },
  {
    id: "alert-always-has-title",
    component: "Alert",
    rule: "Alerts must always include an AlertTitle.",
    rationale: "The title is the primary communication. Description is supplementary.",
    severity: "error"
  },
  // ─── Dialog Rules ───
  {
    id: "dialog-footer-primary-right",
    component: "DialogFooter",
    rule: "Primary action button must be the rightmost button in the footer.",
    rationale: "Consistent with platform convention. Cancel/secondary on the left, confirm/primary on the right.",
    severity: "error"
  },
  {
    id: "dialog-close-always-visible",
    component: "Dialog",
    rule: "Non-alert dialogs must always have a close button (showCloseButton=true).",
    rationale: "Users need an escape hatch. Only AlertDialogs (which require an explicit decision) can omit the close button.",
    severity: "error"
  },
  // ─── Input Rules ───
  {
    id: "input-icon-left-only",
    component: "InputGroup",
    rule: "Decorative icons go on the left (inline-start). Right side is reserved for status indicators and action buttons.",
    rationale: "Left icon identifies the input purpose (search, filter). Right side is for interactive elements (clear, validation status).",
    severity: "warning"
  },
  // ─── Navbar Rules ───
  {
    id: "navbar-item-always-has-icon",
    component: "NavbarItem",
    rule: "Navbar items must always include an icon.",
    rationale: "The sidebar is a constrained space where icons serve as primary recognition. Label-only items look broken.",
    severity: "error"
  },
  // ─── Table Rules ───
  {
    id: "table-header-always-sortable-indicator",
    component: "TableHead",
    rule: "If a column is sortable, always show the TableSortButton, even when unsorted.",
    rationale: "The sort icon indicates sortability. Hiding it until sorted means users can't discover which columns are sortable.",
    severity: "warning"
  },
  // ─── General Rules ───
  {
    id: "no-hardcoded-colors",
    component: "*",
    rule: "Never use hardcoded hex colors. Always use semantic tokens (bg-primary, text-foreground, etc.).",
    rationale: "Hardcoded colors break dark mode and theming.",
    severity: "error"
  },
  {
    id: "icon-size-never-inline",
    component: "*",
    rule: "Never set icon size inline (width/height attributes). Use the parent's [&_svg] selector or size-4 class.",
    rationale: "Icons inherit their size from the parent component. Inline sizing fights the design system.",
    severity: "error"
  }
];

// src/rules/layout-rules.ts
var layoutRules = {
  /**
   * Spacing rhythm — three tiers of visual hierarchy
   */
  spacing: {
    /**
     * 16px (spacing-md) — DEFAULT gap between distinct sections.
     *
     * Use between:
     * - Page header and control bar
     * - Control bar and table
     * - Table and pagination row
     * - Sidebar sections
     * - Form field groups
     *
     * This is the safe default. When in doubt, use 16px.
     */
    betweenSections: "gap-4",
    // 16px
    /**
     * 8px (spacing-sm) — DEFAULT gap within a component group.
     *
     * Use between:
     * - Items inside a title bar
     * - Buttons inside a control bar
     * - Items inside a table cell
     * - Elements inside a card header
     * - Icon and label inside any component
     *
     * This is the internal rhythm of any single "unit" of UI.
     */
    withinComponent: "gap-2",
    // 8px
    /**
     * 24px (spacing-lg) — Visual breathing room between dense blocks.
     *
     * Use between:
     * - Dense information sections in a sidebar detail panel
     * - Bounded card sections on the main page
     * - Major content zones that need visual separation
     *
     * Less common. Only when 16px doesn't create enough separation.
     */
    betweenDenseBlocks: "gap-6",
    // 24px
    /**
     * 1px, 2px, 4px — ONLY inside controls.
     *
     * These micro-spacings are for:
     * - Padding inside form controls (py-0, py-0.5)
     * - Gap between icon and text inside a button (gap-1 for sm, gap-2 for md)
     * - Segment control item padding
     *
     * Never use these for page layout or between components.
     */
    insideControls: "gap-0.5 | gap-1 | px-1 | py-0.5"
  },
  /**
   * Page padding — content surface insets
   */
  pagePadding: {
    /**
     * Standard page content padding.
     * Page header: px-4 py-3 (horizontal 16px, vertical 12px)
     * Page body: px-6 py-4 (horizontal 24px, vertical 16px)
     */
    header: "px-4 py-3",
    body: "px-6 py-4"
  },
  /**
   * Platform shell — fixed layout dimensions
   */
  shell: {
    headerHeight: "h-12",
    // 48px
    navbarWidth: "w-[180px]",
    // 180px fixed
    contentRadius: "rounded-[8px]",
    contentMargin: "pb-2 pr-2",
    // 8px bottom and right
    shellBg: "bg-muted",
    contentBg: "bg-background"
  },
  /**
   * Common layout patterns
   */
  patterns: {
    /** Page header → Control bar → Content → Pagination */
    listPage: {
      headerToControls: "gap-4",
      // 16px
      controlsToTable: "gap-4",
      // 16px
      tableToPagination: "gap-4"
      // 16px
    },
    /** Detail page with sidebar */
    detailPage: {
      mainToSidebar: "gap-6",
      // 24px
      sidebarSections: "gap-6",
      // 24px between info blocks
      sidebarWithin: "gap-2"
      // 8px within each block
    },
    /** Form layout */
    form: {
      fieldGroups: "gap-4",
      // 16px between field groups
      labelToInput: "gap-1.5",
      // 6px between label and input
      inputToHelper: "gap-1"
      // 4px between input and helper text
    }
  }
};

export { AspectRatio, ButtonGroup, ButtonGroupSeparator, ButtonGroupText, ChartContainer, ChartLegend, ChartLegendContent, ChartStyle, ChartTooltip, ChartTooltipContent, Collapsible, CollapsibleContent, CollapsibleTrigger3 as CollapsibleTrigger, ContextMenu, ContextMenuCheckboxItem, ContextMenuContent, ContextMenuEmpty, ContextMenuFooter, ContextMenuGroup, ContextMenuItem, ContextMenuItemBadge, ContextMenuItemDescription, ContextMenuItemIcon, ContextMenuLabel, ContextMenuLoading, ContextMenuPortal, ContextMenuRadioGroup, ContextMenuRadioItem, ContextMenuSearch, ContextMenuSeparator, ContextMenuShortcut, ContextMenuSub, ContextMenuSubContent, ContextMenuSubTrigger, ContextMenuTrigger3 as ContextMenuTrigger, DataTreeView, Field, FieldContent, FieldDescription, FieldError, FieldGroup, FieldLabel, FieldLegend, FieldSeparator, FieldSet, FieldTitle, FileTreeView, Item, ItemActions, ItemContent, ItemDescription, ItemFooter, ItemGroup, ItemHeader, ItemMedia, ItemSeparator, ItemTitle, Kbd, KbdGroup, Menubar3 as Menubar, MenubarCheckboxItem, MenubarContent2 as MenubarContent, MenubarEmpty, MenubarFooter, MenubarGroup, MenubarItem, MenubarItemBadge, MenubarItemDescription, MenubarItemIcon, MenubarLabel, MenubarLoading, MenubarMenu, MenubarPortal, MenubarRadioGroup, MenubarRadioItem, MenubarSearch, MenubarSeparator, MenubarShortcut, MenubarSub, MenubarSubContent, MenubarSubTrigger, MenubarTrigger, NativeSelect, NativeSelectOptGroup, NativeSelectOption, PageHeader, PageHeaderActions, PageHeaderBack, PageHeaderTitle, PageHeaderTitleBar, PlatformHeader, PlatformHeaderBadge, PlatformHeaderCenter, PlatformHeaderLeft, PlatformHeaderRight, ResizableHandle, ResizablePanel, ResizablePanelGroup, ScrollArea, ScrollBar, ToggleGroup2 as ToggleGroup, ToggleGroupItem, Tree, TreeNode, TreeNodeRenderer, TreeNodeTag, TreePreviewAnchorContext, TreeSection, buttonGroupVariants, compositionRules, layoutRules };
//# sourceMappingURL=index.mjs.map
//# sourceMappingURL=index.mjs.map