node_modules
.next
out
dist
# Exception: ship the pre-built DBUI dist/ in git so consumers in
# environments without npm registry access (e.g. Databricks corp network)
# can clone and use DBUI without running `npm install`.
!packages/dbui/dist/
!packages/dbui/dist/**
.env
.env.local
.yarn/cache
.yarn/install-state.gz
*.tsbuildinfo
storybook-static

# Build artifacts (not distributed)
packages/dbui/tsup.config.ts
packages/dbui/design-tokens.json

# Repo-internal (force-add with git add -f)
figma.config.json
.claude/

# Maintainer-only working notes & one-shot audits.
# Never distributed to consumers; preserved locally for reference until
# the L2 auto-generation pipeline replaces the manually-curated lookups.
project/

# apps/portal build artifacts (the app itself IS tracked)
apps/portal/.next/
apps/portal/node_modules/
apps/portal/storybook-static/
apps/portal/out/
